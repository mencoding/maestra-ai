# v0.6.2-alpha.0 Minors Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fechar os 8 Minors do review pós-v0.6.0-alpha.0 — narrowing de `except Exception` para respeitar o pipeline central de `MaestraError` + hardening (singleton reset, anti-loop cap) + docstring.

**Architecture:** Substituir `except Exception` genérico por `except MaestraError: raise` + fallback narrow; remover wrappers de `try/except` em 5 handlers do CLI que sabotam o pipeline central em `cli/__init__.py:246`; trocar globals de rate limiter por dict keyed por `db_path`; adicionar cap de 200 páginas em paginação de playlists; 1 docstring em audit.

**Tech Stack:** Python 3.11+, `pytest` com mocks (`unittest.mock`). Zero dep nova.

**Spec:** `docs/superpowers/specs/2026-04-19-v062-minors-design.md`.

**Base antes de começar:** tag `v0.6.1-alpha.0` + commit `b3df2b5`. Suite: 467 maestra-ai + 39 maestra-mcp = 506.

---

## File Structure

| Arquivo | Tipo | Responsabilidade na v0.6.2 |
|---|---|---|
| `packages/maestra-ai/src/maestra_ai/core/audit.py` | Modify | M5 docstring em `_redact` |
| `packages/maestra-ai/src/maestra_ai/core/onboard.py` | Modify | M1 narrowing, M2 narrowing, M6 cap |
| `packages/maestra-ai/src/maestra_ai/core/client.py` | Modify | M4 dict cache `_BUCKETS`/`_BREAKERS` |
| `packages/maestra-ai/src/maestra_ai/cli/basic.py` | Modify | M3 ConfigError narrowing, M7 remover 5 wrappers + ajuste cmd_queue_context |
| `packages/maestra-ai/src/maestra_ai/cli/onboard.py` | Modify | M8 SystemExit → SpotifyAPIError |
| `packages/maestra-ai/tests/unit/test_onboard.py` | Add | TestResolvePlaylistName, TestFetchOwnPlaylistsCap |
| `packages/maestra-ai/tests/unit/test_client.py` | Add | TestBucketCache |
| `packages/maestra-ai/tests/unit/test_cli_basic.py` | Modify+Add | TestCmdPlayPropagaMaestraError, TestQueueContextFailedField |
| `packages/maestra-ai/tests/unit/test_cli_onboard.py` | Add | TestInteractiveChoosePropaga |
| `packages/maestra-ai/pyproject.toml` | Modify | bump 0.6.1a0 → 0.6.2a0 |
| `packages/maestra-mcp/pyproject.toml` | Modify | bump + pin |
| `CHANGELOG.md` | Add | seção [0.6.2-alpha.0] |
| `docs/reviews/2026-04-19-v060-post-release.md` | Touch | marcar M1-M8 fechados |

---

## Task 1: M5 — docstring em `audit._redact`

**Files:**
- Modify: `packages/maestra-ai/src/maestra_ai/core/audit.py:34-39`

- [ ] **Step 1: Adicionar docstring**

Em `packages/maestra-ai/src/maestra_ai/core/audit.py`, achar:

```python
def _redact(data: Any) -> Any:
    if isinstance(data, dict):
        return {k: ("REDACTED" if k.lower() in _SECRET_KEYS else _redact(v)) for k, v in data.items()}
    if isinstance(data, list):
        return [_redact(x) for x in data]
    return data
```

Substituir por:

```python
def _redact(data: Any) -> Any:
    """Redige valores de chaves sensíveis.

    Chaves em `_SECRET_KEYS` casam por igualdade exata (lowercase).
    Variantes como `"user_email"` ou `"refresh_token_ttl"` NÃO casam
    — se precisar redactar, adicionar explicitamente ao set.
    """
    if isinstance(data, dict):
        return {k: ("REDACTED" if k.lower() in _SECRET_KEYS else _redact(v)) for k, v in data.items()}
    if isinstance(data, list):
        return [_redact(x) for x in data]
    return data
```

- [ ] **Step 2: Rodar testes de audit**

Run: `cd /home/menzani/Desenvolvimento/maestra-ai/packages/maestra-ai && uv run pytest tests/unit/test_audit.py 2>&1 | tail -3`
Expected: todos os testes passam (sem mudança de comportamento).

- [ ] **Step 3: Commit**

```bash
cd /home/menzani/Desenvolvimento/maestra-ai
git add packages/maestra-ai/src/maestra_ai/core/audit.py
git commit -m "docs(audit): explicitar exact-match lowercase em _redact (M5 v0.6.2)"
```

---

## Task 2: M6 — cap de 200 páginas em `_fetch_own_playlists` (TDD)

**Files:**
- Modify: `packages/maestra-ai/src/maestra_ai/core/onboard.py`
- Modify: `packages/maestra-ai/tests/unit/test_onboard.py`

- [ ] **Step 1: Escrever o teste failing**

Em `packages/maestra-ai/tests/unit/test_onboard.py`, adicionar uma nova classe no nível de módulo (próximo a `TestFetchOwnPlaylists`):

```python
class TestFetchOwnPlaylistsCap:
    """M6 v0.6.2: _fetch_own_playlists tem cap de 200 páginas contra
    loop infinito (bug de servidor, mock quebrado, Spotify retornando
    `next` indefinidamente)."""

    def test_para_em_200_paginas_mesmo_com_next_infinito(self):
        from unittest.mock import MagicMock
        sp = MagicMock()
        call_count = {"n": 0}

        def _pages(limit, offset):
            call_count["n"] += 1
            return {
                "items": [
                    {
                        "id": f"p{offset}",
                        "name": f"pl{offset}",
                        "owner": {"id": "me"},
                        "tracks": {"total": 3},
                    },
                ],
                "next": "https://api.spotify.com/next-page-fake",  # nunca None
            }

        sp.current_user_playlists.side_effect = _pages

        collected, _empty = onboard._fetch_own_playlists(sp, me_id="me")
        # Cap atinge: no máximo 200 chamadas.
        assert call_count["n"] <= 200
        # E o resultado tem 200 playlists (uma por página), não loop infinito.
        assert len(collected) == 200
```

Verificar que `onboard` está importado no topo do arquivo. Se não, adicionar `from maestra_ai.core import onboard`.

- [ ] **Step 2: Rodar e confirmar não-terminação (timeout curto)**

Run: `cd /home/menzani/Desenvolvimento/maestra-ai/packages/maestra-ai && timeout 30 uv run pytest tests/unit/test_onboard.py::TestFetchOwnPlaylistsCap -v 2>&1 | tail -10`
Expected: o comando faz timeout em 30s OU falha com uma assert — ambos provam que não há cap hoje. (Se o mock `.side_effect` acaba quando `StopIteration` dispara, o teste vai falhar com RuntimeError/StopIteration; isso também é aceitável como "FAIL" — o ponto é que o cap ainda não existe.)

- [ ] **Step 3: Implementar o cap**

Em `packages/maestra-ai/src/maestra_ai/core/onboard.py`, achar (em torno da linha 122):

```python
def _fetch_own_playlists(
    sp, me_id: str, *, progress_cb: Callable | None = None,
) -> tuple[list[OwnPlaylist], int]:
    ...
    collected: list[dict] = []
    empty_count = 0
    offset = 0
    while True:
        resp = sp.current_user_playlists(limit=_PAGE, offset=offset)
        items = resp.get("items", []) or []
        if not items:
            break
        for it in items:
            ...
        offset += len(items)
        if progress_cb:
            progress_cb(len(collected))
        if resp.get("next") is None:
            break
    return collected, empty_count
```

Substituir `while True:` por `for _ in range(_PLAYLIST_PAGE_CAP):` e adicionar a constante `_PLAYLIST_PAGE_CAP` logo antes da função:

```python
# M6 v0.6.2: cap anti-loop. 200 × _PAGE(50) = 10_000 playlists
# (Spotify hard limit para conta de usuário). Defense-in-depth contra
# bug de servidor ou mock com `next` fixo.
_PLAYLIST_PAGE_CAP = 200


def _fetch_own_playlists(
    sp, me_id: str, *, progress_cb: Callable | None = None,
) -> tuple[list[OwnPlaylist], int]:
    ...
    collected: list[dict] = []
    empty_count = 0
    offset = 0
    for _ in range(_PLAYLIST_PAGE_CAP):
        resp = sp.current_user_playlists(limit=_PAGE, offset=offset)
        items = resp.get("items", []) or []
        if not items:
            break
        for it in items:
            ...  # lógica inalterada
        offset += len(items)
        if progress_cb:
            progress_cb(len(collected))
        if resp.get("next") is None:
            break
    return collected, empty_count
```

O bloco `for it in items: ...` (lógica de filtro por owner e track_count) fica inalterado — apenas o `while True:` vira `for _ in range(_PLAYLIST_PAGE_CAP):`.

- [ ] **Step 4: Rodar teste — confirmar PASS**

Run: `cd /home/menzani/Desenvolvimento/maestra-ai/packages/maestra-ai && uv run pytest tests/unit/test_onboard.py::TestFetchOwnPlaylistsCap -v 2>&1 | tail -5`
Expected: 1 passed.

- [ ] **Step 5: Rodar suite de onboard completa (regressão)**

Run: `cd /home/menzani/Desenvolvimento/maestra-ai/packages/maestra-ai && uv run pytest tests/unit/test_onboard.py 2>&1 | tail -3`
Expected: todos os testes passam.

- [ ] **Step 6: Commit**

```bash
cd /home/menzani/Desenvolvimento/maestra-ai
git add packages/maestra-ai/src/maestra_ai/core/onboard.py packages/maestra-ai/tests/unit/test_onboard.py
git commit -m "fix(onboard): cap de 200 páginas em _fetch_own_playlists (M6 v0.6.2)

Defense-in-depth contra loop infinito quando Spotify retorna 'next'
fixo (bug de servidor ou mock). 200 × 50 = 10_000 playlists (hard
limit por conta). Padrão já usado em _fetch_saved."
```

---

## Task 3: M4 — dict cache em `client.py` (TDD)

**Files:**
- Modify: `packages/maestra-ai/src/maestra_ai/core/client.py:18-39`
- Modify: `packages/maestra-ai/tests/unit/test_client.py`

- [ ] **Step 1: Escrever o teste failing**

Em `packages/maestra-ai/tests/unit/test_client.py`, adicionar:

```python
class TestBucketCache:
    """M4 v0.6.2: rate limiter singletons são cacheados por db_path,
    não por global mutável. Permite testes com tmp_path distintos
    sem reset manual."""

    def test_get_bucket_instancia_nova_quando_db_path_muda(
        self, tmp_path, monkeypatch,
    ):
        from maestra_ai.core import client

        monkeypatch.setenv("MAESTRA_STATE_DIR", str(tmp_path / "a"))
        b1 = client._get_bucket()

        monkeypatch.setenv("MAESTRA_STATE_DIR", str(tmp_path / "b"))
        b2 = client._get_bucket()

        assert id(b1) != id(b2), (
            "bucket deveria ser uma instância nova quando o db_path muda"
        )

    def test_get_bucket_mesma_instancia_para_mesmo_db_path(
        self, tmp_path, monkeypatch,
    ):
        from maestra_ai.core import client

        monkeypatch.setenv("MAESTRA_STATE_DIR", str(tmp_path / "a"))
        b1 = client._get_bucket()
        b2 = client._get_bucket()
        assert id(b1) == id(b2), (
            "chamadas repetidas com mesmo db_path devem retornar o cacheado"
        )
```

- [ ] **Step 2: Rodar e confirmar FAIL**

Run: `cd /home/menzani/Desenvolvimento/maestra-ai/packages/maestra-ai && uv run pytest tests/unit/test_client.py::TestBucketCache -v 2>&1 | tail -10`
Expected: `test_get_bucket_instancia_nova_quando_db_path_muda` FALHA (global `_bucket` gruda na primeira instância). O 2º teste passa. Se passar os dois por acaso (dependendo de ordem de execução anterior), rodar com `-p no:cacheprovider --forked` ou limpar globals manualmente — alternativamente, resetar o global antes:

Se ambos passarem sem o fix (ordem frágil), adicione ao setup do 1º teste:
```python
        import maestra_ai.core.client as _client
        _client._bucket = None  # reset manual para provar o ponto
        _client._breaker = None
```
E o teste ainda deve falhar porque a 2ª chamada vai retornar a instância cacheada da 1ª setenv (path "a"), não criar uma nova para path "b". Se ainda assim passar, o bug foi fixado sem querer — pule para step 4.

- [ ] **Step 3: Implementar dict cache**

Em `packages/maestra-ai/src/maestra_ai/core/client.py`, achar:

```python
# Singletons compartilhados entre daemon (director run) e CLI manual via SQLite.
# Preguiçosos para respeitar MAESTRA_STATE_DIR setado em testes via monkeypatch.
_bucket: PersistentTokenBucket | None = None
_breaker: PersistentCircuitBreaker | None = None


def _get_bucket() -> PersistentTokenBucket:
    global _bucket
    if _bucket is None:
        _bucket = PersistentTokenBucket(
            capacity=60, refill_per_sec=1.0, db_path=_ratelimit_db_path()
        )
    return _bucket


def _get_breaker() -> PersistentCircuitBreaker:
    global _breaker
    if _breaker is None:
        _breaker = PersistentCircuitBreaker(
            max_failures=3, window_sec=60, cooldown_sec=300, db_path=_ratelimit_db_path()
        )
    return _breaker
```

Substituir por:

```python
# M4 v0.6.2: cache por db_path em vez de singletons globais. Testes que
# mudam MAESTRA_STATE_DIR entre cenários ganham instâncias novas sem
# reset manual; produção tem 1 db_path → dict de 1 entry (overhead nulo).
_BUCKETS: dict[str, PersistentTokenBucket] = {}
_BREAKERS: dict[str, PersistentCircuitBreaker] = {}


def _get_bucket() -> PersistentTokenBucket:
    path = _ratelimit_db_path()
    if path not in _BUCKETS:
        _BUCKETS[path] = PersistentTokenBucket(
            capacity=60, refill_per_sec=1.0, db_path=path
        )
    return _BUCKETS[path]


def _get_breaker() -> PersistentCircuitBreaker:
    path = _ratelimit_db_path()
    if path not in _BREAKERS:
        _BREAKERS[path] = PersistentCircuitBreaker(
            max_failures=3, window_sec=60, cooldown_sec=300, db_path=path
        )
    return _BREAKERS[path]
```

- [ ] **Step 4: Rodar testes — confirmar PASS**

Run: `cd /home/menzani/Desenvolvimento/maestra-ai/packages/maestra-ai && uv run pytest tests/unit/test_client.py::TestBucketCache -v 2>&1 | tail -5`
Expected: 2 passed.

- [ ] **Step 5: Rodar suite de client completa**

Run: `cd /home/menzani/Desenvolvimento/maestra-ai/packages/maestra-ai && uv run pytest tests/unit/test_client.py 2>&1 | tail -3`
Expected: todos os testes passam.

- [ ] **Step 6: Commit**

```bash
cd /home/menzani/Desenvolvimento/maestra-ai
git add packages/maestra-ai/src/maestra_ai/core/client.py packages/maestra-ai/tests/unit/test_client.py
git commit -m "refactor(client): dict cache por db_path em rate limiter (M4 v0.6.2)

Substitui globals _bucket/_breaker por _BUCKETS/_BREAKERS keyed por
db_path. Testes com MAESTRA_STATE_DIR variável ganham instâncias
novas sem reset manual. Produção: 1 db_path → overhead nulo."
```

---

## Task 4: M1 — narrowing em `_resolve_playlist_name` (TDD)

**Files:**
- Modify: `packages/maestra-ai/src/maestra_ai/core/onboard.py:242-254`
- Modify: `packages/maestra-ai/tests/unit/test_onboard.py`

- [ ] **Step 1: Escrever o teste failing**

Em `packages/maestra-ai/tests/unit/test_onboard.py`, adicionar:

```python
class TestResolvePlaylistName:
    """M1 v0.6.2: _resolve_playlist_name propaga MaestraError em vez
    de engolir; fallback só para shape inesperado."""

    def test_propaga_auth_error_e_nao_silencia(self):
        from unittest.mock import MagicMock
        import pytest
        from maestra_ai.core.errors import AuthError

        sp = MagicMock()
        sp.current_user_playlists.side_effect = AuthError("token revogado")

        with pytest.raises(AuthError, match="token revogado"):
            onboard._resolve_playlist_name(sp, "Maestra")

    def test_propaga_rate_limit_error(self):
        from unittest.mock import MagicMock
        import pytest
        from maestra_ai.core.errors import RateLimitError

        sp = MagicMock()
        sp.current_user_playlists.side_effect = RateLimitError(
            "429", retry_after=30,
        )
        with pytest.raises(RateLimitError):
            onboard._resolve_playlist_name(sp, "Maestra")

    def test_retorna_desired_quando_shape_inesperada(self):
        """AttributeError/TypeError em resp.get(...) → fallback benigno."""
        from unittest.mock import MagicMock

        sp = MagicMock()
        # current_user_playlists retorna algo sem .get (simula bug de API)
        sp.current_user_playlists.return_value = object()
        assert onboard._resolve_playlist_name(sp, "Maestra") == "Maestra"
```

- [ ] **Step 2: Rodar — confirmar FAIL nos dois primeiros**

Run: `cd /home/menzani/Desenvolvimento/maestra-ai/packages/maestra-ai && uv run pytest tests/unit/test_onboard.py::TestResolvePlaylistName -v 2>&1 | tail -10`
Expected: `test_propaga_auth_error_e_nao_silencia` FAIL (AuthError engolido pelo `except Exception`; função retorna "Maestra"). `test_propaga_rate_limit_error` idem. Terceiro PASS.

- [ ] **Step 3: Implementar narrowing**

Em `packages/maestra-ai/src/maestra_ai/core/onboard.py:242-254`, achar:

```python
def _resolve_playlist_name(sp, desired: str) -> str:
    """Se já existe playlist com esse nome, acrescenta sufixo numérico."""
    try:
        existing = sp.current_user_playlists(limit=50).get("items", [])
    except Exception:
        return desired
    existing_names = {p.get("name") for p in existing}
    ...
```

Substituir o `try/except` por:

```python
def _resolve_playlist_name(sp, desired: str) -> str:
    """Se já existe playlist com esse nome, acrescenta sufixo numérico."""
    from maestra_ai.core.errors import MaestraError
    try:
        existing = sp.current_user_playlists(limit=50).get("items", [])
    except MaestraError:
        raise  # AuthError/RateLimit/API → pipeline central
    except Exception:
        return desired  # shape inesperada da API → fallback benigno
    existing_names = {p.get("name") for p in existing}
    ...
```

Se `MaestraError` já estiver importado no topo do arquivo, dispensa o import local — mover para topo.

- [ ] **Step 4: Rodar — confirmar PASS**

Run: `cd /home/menzani/Desenvolvimento/maestra-ai/packages/maestra-ai && uv run pytest tests/unit/test_onboard.py::TestResolvePlaylistName -v 2>&1 | tail -5`
Expected: 3 passed.

- [ ] **Step 5: Commit**

```bash
cd /home/menzani/Desenvolvimento/maestra-ai
git add packages/maestra-ai/src/maestra_ai/core/onboard.py packages/maestra-ai/tests/unit/test_onboard.py
git commit -m "fix(onboard): narrowing em _resolve_playlist_name (M1 v0.6.2)

Separa 'except MaestraError: raise' de 'except Exception: return
desired'. AuthError/RateLimit/API propagam ao pipeline central
(cli/__init__.py:246) em vez de serem engolidos. Fallback mantido
apenas para shape inesperada da API."
```

---

## Task 5: M2 — narrowing em `onboard.run` (bloco _fetch_own_playlists)

**Files:**
- Modify: `packages/maestra-ai/src/maestra_ai/core/onboard.py:471-480`

- [ ] **Step 1: Aplicar narrowing**

Em `packages/maestra-ai/src/maestra_ai/core/onboard.py`, achar:

```python
        try:
            me = sp.current_user()
            own_playlists, empty_count = _fetch_own_playlists(
                sp, me_id=me.get("id"),
                progress_cb=(
                    lambda n: report_step(6, "Listando playlists", f"{n}")
                ) if progress_cb else None,
            )
        except Exception:
            own_playlists, empty_count = [], 0
```

Substituir o `except Exception:` por duplo catch:

```python
        try:
            me = sp.current_user()
            own_playlists, empty_count = _fetch_own_playlists(
                sp, me_id=me.get("id"),
                progress_cb=(
                    lambda n: report_step(6, "Listando playlists", f"{n}")
                ) if progress_cb else None,
            )
        except MaestraError:
            raise  # AuthError/RateLimit/API → pipeline central
        except Exception:
            own_playlists, empty_count = [], 0  # shape inesperada → fallback
```

Verificar que `MaestraError` está importado no topo do arquivo. Se não, adicionar `from maestra_ai.core.errors import MaestraError` no topo (junto dos outros imports de `errors`).

- [ ] **Step 2: Rodar suite de onboard completa**

Run: `cd /home/menzani/Desenvolvimento/maestra-ai/packages/maestra-ai && uv run pytest tests/unit/test_onboard.py 2>&1 | tail -3`
Expected: todos os testes passam. Se algum teste específico de `TestPlaylistExpansion` falhar porque esperava silêncio de AuthError, adicionar caso explícito:

```python
class TestExpansionPropagaAuthError:
    def test_auth_error_durante_fetch_playlists_nao_eh_silenciado(
        self, tmp_path, monkeypatch,
    ):
        """M2 v0.6.2: AuthError em _fetch_own_playlists propaga;
        não vira 'no_own_playlists' silencioso."""
        import pytest
        from maestra_ai.core.errors import AuthError
        from maestra_ai.core.taste import TasteProfile

        monkeypatch.setenv("MAESTRA_CONFIG_DIR", str(tmp_path))
        monkeypatch.setenv("MAESTRA_DATA_DIR", str(tmp_path / "data"))

        sp = TestPlaylistExpansion()._sp_with_playlists()
        sp.current_user_playlists.side_effect = AuthError("revoked")

        taste = TasteProfile(tmp_path / "taste.json")
        with pytest.raises(AuthError, match="revoked"):
            onboard.run(
                sp, taste, playlist_name="M", seed_count=0,
                playlist_selector=lambda pls, ctx: [],
                total_cap=5000,
            )
```

Adicionar essa classe em `test_onboard.py` próximo a `TestPlaylistExpansion`. Rodar especifico:
`cd /home/menzani/Desenvolvimento/maestra-ai/packages/maestra-ai && uv run pytest tests/unit/test_onboard.py::TestExpansionPropagaAuthError -v 2>&1 | tail -5`
Expected: 1 passed.

- [ ] **Step 3: Commit**

```bash
cd /home/menzani/Desenvolvimento/maestra-ai
git add packages/maestra-ai/src/maestra_ai/core/onboard.py packages/maestra-ai/tests/unit/test_onboard.py
git commit -m "fix(onboard): narrowing em run() _fetch_own_playlists (M2 v0.6.2)

Substitui 'except Exception' genérico por duplo-catch: MaestraError
propaga ao pipeline central, shape inesperada vira fallback silencioso.
Novo teste TestExpansionPropagaAuthError garante regressão."
```

---

## Task 6: M3 — narrowing em `cmd_status` (ConfigError)

**Files:**
- Modify: `packages/maestra-ai/src/maestra_ai/cli/basic.py:164-167`

- [ ] **Step 1: Aplicar narrowing**

Em `packages/maestra-ai/src/maestra_ai/cli/basic.py`, achar:

```python
def cmd_status(args, controller, taste, context_state, feedback_prompter, **_):
    active_context = context_state.show()
    context = active_context["context"] if active_context else None
    try:
        playlist_id = resolve_playlist_id()
    except Exception:
        playlist_id = None
```

Substituir `except Exception:` por `except ConfigError:`:

```python
def cmd_status(args, controller, taste, context_state, feedback_prompter, **_):
    active_context = context_state.show()
    context = active_context["context"] if active_context else None
    try:
        playlist_id = resolve_playlist_id()
    except ConfigError:
        playlist_id = None
```

Verificar que `ConfigError` está importado no arquivo. Se não, adicionar `from maestra_ai.core.errors import ConfigError` no topo. Se outros erros já forem importados (`from maestra_ai.core.errors import ...`), adicionar `ConfigError` à lista.

- [ ] **Step 2: Rodar suite CLI basic**

Run: `cd /home/menzani/Desenvolvimento/maestra-ai/packages/maestra-ai && uv run pytest tests/unit/test_cli_basic.py 2>&1 | tail -3`
Expected: todos os testes passam.

- [ ] **Step 3: Commit**

```bash
cd /home/menzani/Desenvolvimento/maestra-ai
git add packages/maestra-ai/src/maestra_ai/cli/basic.py
git commit -m "fix(cli/basic): cmd_status captura apenas ConfigError (M3 v0.6.2)

Era 'except Exception' — engolia qualquer erro não relacionado a
config.json. Agora só ConfigError; o resto propaga ao main()."
```

---

## Task 7: M7 — remover wrappers de exceção em 5 handlers (TDD)

**Files:**
- Modify: `packages/maestra-ai/src/maestra_ai/cli/basic.py`
- Modify: `packages/maestra-ai/tests/unit/test_cli_basic.py`

- [ ] **Step 1: Escrever os testes failing**

Em `packages/maestra-ai/tests/unit/test_cli_basic.py`, adicionar:

```python
class TestCmdPlayPropagaMaestraError:
    """M7 v0.6.2: handlers de CLI não engolem MaestraError em str(e).
    MaestraError deve propagar ao main() para render estruturada."""

    def test_cmd_play_propaga_rate_limit_error(self):
        from unittest.mock import MagicMock
        import pytest
        from maestra_ai.cli.basic import cmd_play
        from maestra_ai.core.errors import RateLimitError

        controller = MagicMock()
        controller.play.side_effect = RateLimitError("429", retry_after=30)
        args = MagicMock(uri=None, human=False, json=True)

        with pytest.raises(RateLimitError):
            cmd_play(args, controller=controller)

    def test_cmd_queue_add_propaga_auth_error(self):
        from unittest.mock import MagicMock
        import pytest
        from maestra_ai.cli.basic import cmd_queue_add
        from maestra_ai.core.errors import AuthError

        controller = MagicMock()
        controller.queue_add.side_effect = AuthError("token expirou")
        args = MagicMock(uri="spotify:track:xxx", human=False, json=True)

        with pytest.raises(AuthError, match="token expirou"):
            cmd_queue_add(args, controller=controller)


class TestQueueContextFailedField:
    """M7 v0.6.2: cmd_queue_context mantém try/except por-track MAS
    usa MaestraError e registra no campo 'failed' em vez de abortar
    o loop com error()."""

    def test_falhas_por_track_registradas_em_failed(self, capsys):
        from unittest.mock import MagicMock
        from maestra_ai.cli.basic import cmd_queue_context
        from maestra_ai.core.errors import SpotifyAPIError

        controller = MagicMock()
        taste = MagicMock()
        curator = MagicMock()
        context_state = MagicMock()
        context_state.show.return_value = {"context": "workout"}

        tracks = [
            {"uri": "spotify:track:t1", "track": "a", "artist": "x"},
            {"uri": "spotify:track:t2", "track": "b", "artist": "y"},
        ]
        curator.curate.return_value = (tracks, ["q1"])

        # Primeira track sucesso, segunda falha com MaestraError.
        def _queue_add(uri):
            if uri == "spotify:track:t2":
                raise SpotifyAPIError("500 server error", status=500)

        controller.queue_add.side_effect = _queue_add

        args = MagicMock(human=False, json=True, count=2)
        cmd_queue_context(
            args,
            controller=controller,
            taste=taste,
            curator=curator,
            context_state=context_state,
        )
        out = capsys.readouterr().out
        import json as _json
        payload = _json.loads(out)
        assert len(payload["added"]) == 1
        assert payload["added"][0]["uri"] == "spotify:track:t1"
        assert len(payload["failed"]) == 1
        assert payload["failed"][0]["uri"] == "spotify:track:t2"
        assert payload["failed"][0]["error"]["code"] == "SpotifyAPIError"
```

Verificar imports no topo. Se `import pytest` faltar, adicionar.

- [ ] **Step 2: Rodar — confirmar FAIL**

Run: `cd /home/menzani/Desenvolvimento/maestra-ai/packages/maestra-ai && uv run pytest tests/unit/test_cli_basic.py::TestCmdPlayPropagaMaestraError tests/unit/test_cli_basic.py::TestQueueContextFailedField -v 2>&1 | tail -15`
Expected: todos FAIL (comportamento atual engole via `error(str(e), "...")` ou SystemExit indireto via `error()`).

- [ ] **Step 3: Implementar — remover 5 wrappers**

Em `packages/maestra-ai/src/maestra_ai/cli/basic.py`, aplicar as seguintes substituições:

**`cmd_play`** (linhas 69-75):
```python
def cmd_play(args, controller, **_):
    try:
        controller.play(uri=args.uri if args.uri else None)
        result = controller.now()
        output(result or {"status": "playing"}, args.human)
    except Exception as e:
        error(str(e), "PLAYBACK_ERROR")
```
Vira:
```python
def cmd_play(args, controller, **_):
    controller.play(uri=args.uri if args.uri else None)
    result = controller.now()
    output(result or {"status": "playing"}, args.human)
```

**`cmd_pause`** (linhas 78-83):
```python
def cmd_pause(args, controller, **_):
    try:
        controller.pause()
        output({"status": "paused"}, args.human)
    except Exception as e:
        error(str(e), "PLAYBACK_ERROR")
```
Vira:
```python
def cmd_pause(args, controller, **_):
    controller.pause()
    output({"status": "paused"}, args.human)
```

**`cmd_next`** (linhas 86-93):
```python
def cmd_next(args, controller, **_):
    try:
        controller.next_track()
        time.sleep(0.5)
        result = controller.now()
        output(result or {"status": "next"}, args.human)
    except Exception as e:
        error(str(e), "PLAYBACK_ERROR")
```
Vira:
```python
def cmd_next(args, controller, **_):
    controller.next_track()
    time.sleep(0.5)
    result = controller.now()
    output(result or {"status": "next"}, args.human)
```

**`cmd_queue_add`** (linhas 108-113):
```python
def cmd_queue_add(args, controller, **_):
    try:
        controller.queue_add(args.uri)
        output({"status": "added", "uri": args.uri}, args.human)
    except Exception as e:
        error(str(e), "QUEUE_ERROR")
```
Vira:
```python
def cmd_queue_add(args, controller, **_):
    controller.queue_add(args.uri)
    output({"status": "added", "uri": args.uri}, args.human)
```

**`cmd_play_context`** (linhas 146-150, apenas o try/except volta de `controller.play`):
```python
    track = results[0]
    try:
        controller.play(track["uri"])
    except Exception as e:
        error(str(e), "PLAYBACK_ERROR")
```
Vira:
```python
    track = results[0]
    controller.play(track["uri"])
```

- [ ] **Step 4: Adaptar `cmd_queue_context` para registrar `failed`**

Em `cli/basic.py`, achar `cmd_queue_context` (linha ~116):

```python
def cmd_queue_context(args, controller, taste, curator, context_state, **_):
    context, context_source = _curation_context(args, context_state)
    results, queries_used = curator.curate(context, count=args.count)
    if not results:
        error("Sem resultados para esse contexto.", "NO_RESULTS")

    added = []
    for track in results:
        try:
            controller.queue_add(track["uri"])
            added.append(track)
        except Exception as e:
            error(str(e), "QUEUE_ERROR")

    _record_curated_tracks(taste, added, context, queries_used)
    output({
        "status": "queued",
        "context": context,
        "context_source": context_source,
        "added": len(added),
        "tracks": added,
    }, args.human)
```

Substituir o loop + output por:

```python
def cmd_queue_context(args, controller, taste, curator, context_state, **_):
    from maestra_ai.core.errors import MaestraError

    context, context_source = _curation_context(args, context_state)
    results, queries_used = curator.curate(context, count=args.count)
    if not results:
        error("Sem resultados para esse contexto.", "NO_RESULTS")

    added = []
    failed = []
    for track in results:
        try:
            controller.queue_add(track["uri"])
            added.append(track)
        except MaestraError as e:
            # M7 v0.6.2: falha em uma track não aborta o batch.
            failed.append({"uri": track["uri"], "error": e.to_human_dict()})

    _record_curated_tracks(taste, added, context, queries_used)
    output({
        "status": "queued",
        "context": context,
        "context_source": context_source,
        "added": len(added),
        "tracks": added,
        "failed": failed,
    }, args.human)
```

- [ ] **Step 5: Rodar testes novos**

Run: `cd /home/menzani/Desenvolvimento/maestra-ai/packages/maestra-ai && uv run pytest tests/unit/test_cli_basic.py::TestCmdPlayPropagaMaestraError tests/unit/test_cli_basic.py::TestQueueContextFailedField -v 2>&1 | tail -10`
Expected: todos os testes novos passam.

- [ ] **Step 6: Rodar suite CLI basic completa**

Run: `cd /home/menzani/Desenvolvimento/maestra-ai/packages/maestra-ai && uv run pytest tests/unit/test_cli_basic.py 2>&1 | tail -5`
Expected: todos os testes passam. Se testes antigos falharem por esperar `error(...)` ou `"PLAYBACK_ERROR"` code no output, adaptar: testes que exercitam handlers com exceção devem agora esperar `pytest.raises(MaestraError)` em vez de assertar sobre stdout. Se o teste antigo chamava handler com exceção e verificava `error_msg`, modificar para `pytest.raises`.

- [ ] **Step 7: Commit**

```bash
cd /home/menzani/Desenvolvimento/maestra-ai
git add packages/maestra-ai/src/maestra_ai/cli/basic.py packages/maestra-ai/tests/unit/test_cli_basic.py
git commit -m "refactor(cli/basic): remover wrappers try/except nos handlers (M7 v0.6.2)

cmd_play/pause/next/queue_add/play_context deixam MaestraError
propagar ao main() para render estruturada com suggested_actions
e agent_hint. cmd_queue_context mantém try/except por-track mas
captura apenas MaestraError e registra falhas em novo campo
'failed' no output em vez de abortar o batch.

Breaking do output JSON: erros de playback agora seguem o schema
to_human_dict do MaestraError (code, title, suggested_actions)
em vez de {error: str, code: PLAYBACK_ERROR}. Pre-alpha, zero
consumidor externo."
```

---

## Task 8: M8 — narrowing em `_interactive_choose` (TDD)

**Files:**
- Modify: `packages/maestra-ai/src/maestra_ai/cli/onboard.py:324-328`
- Modify: `packages/maestra-ai/tests/unit/test_cli_onboard.py`

- [ ] **Step 1: Escrever o teste failing**

Em `packages/maestra-ai/tests/unit/test_cli_onboard.py`, adicionar:

```python
class TestInteractiveChoosePropaga:
    """M8 v0.6.2: _interactive_choose propaga MaestraError em vez de
    SystemExit+print. Erros não-Maestra viram SpotifyAPIError."""

    def test_auth_error_nao_vira_system_exit(self, monkeypatch, capsys):
        from unittest.mock import MagicMock
        import pytest
        from maestra_ai.core.errors import AuthError

        # Simula input do usuário para cair no caminho "listar playlists".
        monkeypatch.setattr("builtins.input", lambda _: "2")

        controller = MagicMock()
        controller.sp.current_user_playlists.side_effect = AuthError(
            "token expirou",
        )

        with pytest.raises(AuthError, match="token expirou"):
            cli_onboard._interactive_choose(controller)

    def test_erro_generico_vira_spotify_api_error(self, monkeypatch):
        from unittest.mock import MagicMock
        import pytest
        from maestra_ai.core.errors import SpotifyAPIError

        monkeypatch.setattr("builtins.input", lambda _: "2")

        controller = MagicMock()
        controller.sp.current_user_playlists.side_effect = RuntimeError(
            "unknown failure",
        )

        with pytest.raises(SpotifyAPIError, match="unknown failure"):
            cli_onboard._interactive_choose(controller)
```

Verificar: nome da função é `_interactive_choose` — confirmar no arquivo. Se for diferente (ex: `_prompt_playlist_choice`), adaptar.

- [ ] **Step 2: Rodar — confirmar FAIL**

Run: `cd /home/menzani/Desenvolvimento/maestra-ai/packages/maestra-ai && uv run pytest tests/unit/test_cli_onboard.py::TestInteractiveChoosePropaga -v 2>&1 | tail -10`
Expected: primeiro FALHA com `SystemExit` (engole AuthError); segundo FALHA com `SystemExit` (não traduz para SpotifyAPIError).

- [ ] **Step 3: Implementar narrowing**

Em `packages/maestra-ai/src/maestra_ai/cli/onboard.py:324-328`, achar:

```python
    if escolha == "2":
        # Lista até 20 playlists do usuário.
        try:
            resp = controller.sp.current_user_playlists(limit=20)
        except Exception as e:
            print(f"Erro ao listar playlists: {e}")
            raise SystemExit(1) from e
```

Substituir por:

```python
    if escolha == "2":
        # Lista até 20 playlists do usuário.
        try:
            resp = controller.sp.current_user_playlists(limit=20)
        except MaestraError:
            raise  # AuthError/RateLimit/API → pipeline central
        except Exception as e:
            from maestra_ai.core.errors import SpotifyAPIError
            raise SpotifyAPIError(
                f"Falha ao listar playlists: {e}",
                where={"step": "interactive_choose"},
            ) from e
```

Verificar que `MaestraError` está importado no topo. Se não, adicionar `from maestra_ai.core.errors import MaestraError`. `SpotifyAPIError` fica como import local dentro do except para manter claro que é fallback.

- [ ] **Step 4: Rodar — confirmar PASS**

Run: `cd /home/menzani/Desenvolvimento/maestra-ai/packages/maestra-ai && uv run pytest tests/unit/test_cli_onboard.py::TestInteractiveChoosePropaga -v 2>&1 | tail -5`
Expected: 2 passed.

- [ ] **Step 5: Rodar suite completa CLI onboard**

Run: `cd /home/menzani/Desenvolvimento/maestra-ai/packages/maestra-ai && uv run pytest tests/unit/test_cli_onboard.py 2>&1 | tail -3`
Expected: todos os testes passam.

- [ ] **Step 6: Commit**

```bash
cd /home/menzani/Desenvolvimento/maestra-ai
git add packages/maestra-ai/src/maestra_ai/cli/onboard.py packages/maestra-ai/tests/unit/test_cli_onboard.py
git commit -m "fix(cli/onboard): _interactive_choose propaga MaestraError (M8 v0.6.2)

Substitui 'print + SystemExit(1)' por duplo-catch: MaestraError
propaga ao pipeline central; outras exceções viram SpotifyAPIError
com 'from e' preservando traceback original."
```

---

## Task 9: Bump v0.6.2-alpha.0 + CHANGELOG + backlog + tag

**Files:**
- Modify: `packages/maestra-ai/pyproject.toml`
- Modify: `packages/maestra-mcp/pyproject.toml`
- Modify: `README.md`
- Modify: `CHANGELOG.md`
- Modify: `docs/reviews/2026-04-19-v060-post-release.md`

- [ ] **Step 1: Pre-bump — rodar suite completa**

Run:
```bash
cd /home/menzani/Desenvolvimento/maestra-ai/packages/maestra-ai && uv run pytest tests/unit/ tests/integration/ 2>&1 | tail -3
cd /home/menzani/Desenvolvimento/maestra-ai/packages/maestra-mcp && uv run pytest tests/ 2>&1 | tail -3
```
Expected: maestra-ai ~473 passed (+6 novos), maestra-mcp 39 passed. Zero falhas.

- [ ] **Step 2: Bump maestra-ai/pyproject.toml**

Em `packages/maestra-ai/pyproject.toml`, trocar `version = "0.6.1a0"` → `version = "0.6.2a0"`.

- [ ] **Step 3: Bump maestra-mcp/pyproject.toml**

Em `packages/maestra-mcp/pyproject.toml`:
- Trocar `version = "0.6.1a0"` → `version = "0.6.2a0"`.
- Trocar `"maestra-ai==0.6.1a0"` → `"maestra-ai==0.6.2a0"`.

- [ ] **Step 4: Atualizar README**

Em `README.md`, trocar:
`**Status:** pre-alpha (v0.6.1-alpha). Lançamento público planejado em v1.0.0.`
por:
`**Status:** pre-alpha (v0.6.2-alpha). Lançamento público planejado em v1.0.0.`

- [ ] **Step 5: Adicionar entrada no CHANGELOG**

Em `CHANGELOG.md`, logo após `## [Unreleased]` e antes de `## [0.6.1-alpha.0] - 2026-04-19`, inserir:

```markdown
## [0.6.2-alpha.0] - 2026-04-19

Release só de correção — fecha os 8 Minors do review
pós-v0.6.0-alpha.0. Foco no pipeline central de erros:
`MaestraError` propaga para `main()` em `cli/__init__.py:246` onde
`redact_error_dict` + `_print_rich_error` produzem saída estruturada
com `suggested_actions` e `agent_hint`. Zero feature nova.

### Changed (breaking — output de erros no CLI)
- Handlers `cmd_play`, `cmd_pause`, `cmd_next`, `cmd_queue_add`,
  `cmd_play_context` em `cli/basic.py` não engolem mais
  `MaestraError` em `{"error": str(e), "code": "PLAYBACK_ERROR"}`.
  Erros agora seguem o schema `to_human_dict` (`{"error": {"code":
  "RateLimitError", "title": ..., "suggested_actions": [...],
  "agent_hint": ...}}`). **Zero consumidor externo conhecido
  (pre-alpha); impacto restrito à UX de CLI.**
- `cli/onboard._interactive_choose`: `print` + `SystemExit(1)`
  substituído por propagação de `MaestraError` / tradução para
  `SpotifyAPIError`.
- `core/client.py` — `_bucket`/`_breaker` globals → `_BUCKETS` /
  `_BREAKERS` dict keyed por `db_path`. API pública do módulo
  inalterada (`_get_bucket`/`_get_breaker` permanecem).

### Fixed
- **M1** — `onboard._resolve_playlist_name` agora propaga
  `MaestraError` (AuthError/RateLimit/API) em vez de silenciar.
  Fallback `except Exception: return desired` mantido apenas para
  shape inesperada da API.
- **M2** — mesmo padrão em `onboard.run` no bloco de
  `_fetch_own_playlists`: AuthError propaga; fallback reservado
  para erros de parse.
- **M3** — `cli/basic.cmd_status` captura apenas `ConfigError`
  em vez de `Exception` genérico.
- **M4** — rate limiter resets corretamente quando
  `MAESTRA_STATE_DIR` muda entre testes (dict cache por `db_path`).
- **M6** — `onboard._fetch_own_playlists` tem cap de 200 páginas
  (10_000 playlists, hard limit Spotify). Defense-in-depth contra
  loop infinito por `next` fixo de servidor bugado.
- **M7** — ver Changed.
- **M8** — ver Changed.

### Docs
- **M5** — `audit._redact` docstring explicita exact-match
  lowercase em `_SECRET_KEYS` (variantes como `"user_email"` NÃO
  casam).

### Added
- `cli/basic.cmd_queue_context` output agora inclui campo `failed`
  listando tracks que não puderam ser adicionadas à fila (cada
  entry: `{uri, error: to_human_dict(...)}`). Batch não é mais
  abortado pela primeira falha.

### Tests
- +3 em `TestResolvePlaylistName` (AuthError, RateLimit, shape).
- +1 em `TestFetchOwnPlaylistsCap` (200 páginas).
- +2 em `TestBucketCache` (db_path distintos, mesmo db_path).
- +2 em `TestCmdPlayPropagaMaestraError` (RateLimit, AuthError).
- +1 em `TestQueueContextFailedField` (campo failed preserva batch).
- +2 em `TestInteractiveChoosePropaga` (AuthError, genérico).
- +1 em `TestExpansionPropagaAuthError` (AuthError no fetch).
- Total: ~+12 novos testes.
```

- [ ] **Step 6: Atualizar review backlog**

Em `docs/reviews/2026-04-19-v060-post-release.md`, achar a seção `## Minor (7)` (o título no arquivo diz "(7)" mas listei 8 — está consistente com o review original; o 8 é porque M7 aparece duas vezes). Logo antes desse heading, inserir:

```markdown
> **Status v0.6.2-alpha.0 (2026-04-19):** todos os 8 Minors (M1-M8)
> foram fechados. Detalhes no CHANGELOG [0.6.2-alpha.0].

```

(Blank line após o quote block.)

- [ ] **Step 7: Re-sync workspace**

Run: `cd /home/menzani/Desenvolvimento/maestra-ai && uv sync --all-extras 2>&1 | tail -5`
Expected: `maestra-ai==0.6.2a0` e `maestra-mcp==0.6.2a0` instalados.

- [ ] **Step 8: Validação final**

Run:
```bash
cd /home/menzani/Desenvolvimento/maestra-ai/packages/maestra-ai && uv run pytest tests/unit/ tests/integration/ 2>&1 | tail -3
cd /home/menzani/Desenvolvimento/maestra-ai/packages/maestra-mcp && uv run pytest tests/ 2>&1 | tail -3
```
Expected: maestra-ai ~479 + maestra-mcp 39 = ~518 passed. Zero falhas.

- [ ] **Step 9: Commit + tag**

```bash
cd /home/menzani/Desenvolvimento/maestra-ai
git add -A
git commit -m "chore: bump v0.6.2-alpha.0 — narrowing de exceções + hardening

Fecha os 8 Minors do review pós-v0.6.0-alpha.0:
- M1/M2/M3/M7/M8: narrowing de except Exception → MaestraError propaga
- M4: dict cache por db_path em client.py
- M5: docstring em _redact
- M6: cap 200 páginas em _fetch_own_playlists

Mudança de contrato no output JSON dos handlers de CLI documentada
no CHANGELOG (breaking — output)."
git tag -a v0.6.2-alpha.0 -m "v0.6.2-alpha.0 — narrowing + hardening"
```

---

## Self-Review

### Spec coverage

| Spec § | Task |
|---|---|
| §3.1 padrão narrowing (M1, M2) | Tasks 4, 5 |
| §3.2 M3 narrowing | Task 6 |
| §3.3 M7 remover wrappers | Task 7 |
| §3.3.1 cmd_queue_context failed field | Task 7 Step 4 |
| §3.4 M8 SystemExit → SpotifyAPIError | Task 8 |
| §3.5 M4 dict cache | Task 3 |
| §3.6 M6 cap | Task 2 |
| §3.7 M5 docstring | Task 1 |
| §5 testes | nas respectivas tasks (2, 3, 4, 5, 7, 8) |
| §6 critérios de aceite | Task 9 |

Zero gaps.

### Placeholder scan

Nenhum "TBD"/"TODO"/"similar to Task N". Todos os code blocks completos. Todos os commands com `cd` explícito e saída esperada.

**Exceção consciente em Task 3 Step 2**: a instrução sobre "se ambos testes passarem por acaso, force reset manual" é condicional mas concreta — descreve exatamente como lidar com flakiness de ordem e oferece fallback de `_client._bucket = None` antes do primeiro cenário. Não é placeholder; é defesa.

### Type consistency

- `_PLAYLIST_PAGE_CAP = 200` em Task 2 → usado no mesmo bloco.
- `_BUCKETS: dict[str, PersistentTokenBucket]` / `_BREAKERS` em Task 3 → consistente.
- `MCPInvalidArgsError` não é usado aqui (é do v0.6.1); confirmado.
- `MaestraError` como import comum em Tasks 4, 5, 7, 8 — sempre do mesmo path (`maestra_ai.core.errors`).
- `SpotifyAPIError` com kwarg `where` em Task 8 — assinatura real da classe aceita `where: dict | None` herdado de `MaestraError.__init__`. Consistente.
- Campo `failed: list[dict]` em output de `cmd_queue_context` aparece idêntico em Task 7 Step 4 (implementação) e Task 7 Step 1 (teste).
