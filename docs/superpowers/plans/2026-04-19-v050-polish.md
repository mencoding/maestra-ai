# v0.5.0 Polish — HIGH/MEDIUM Backlog Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fechar os 6 achados HIGH/MEDIUM remanescentes do code review pós-v0.4.3: validação em `taste.restore`, reset manual do circuit breaker, regex estrita do SNAP_ID, redação contextual de secrets, `pgrep` portável, rotação do audit log por tamanho.

**Architecture:** Seis mudanças cirúrgicas, independentes entre si. Cada tarefa vira um commit atômico com TDD estrito (teste primeiro, vê falhar, implementa mínimo, vê passar, commit). Não toca em APIs públicas do CLI ou MCP. Apenas MEDIUM-1 altera comportamento observável (menos falsos positivos em redação). Versão bump 0.4.5 → 0.5.0 justificada por acúmulo de mudanças + breaking mínimo no SNAP_ID (IDs antigos fora do novo padrão ficam inválidos, mas snapshots reais seguem o formato).

**Tech Stack:** Python 3.11+, pytest + pytest-asyncio, freezegun para tempo determinístico, `shutil.which` para portabilidade. Sem dependências novas.

---

## File Structure

| Path | Responsabilidade | Mudança |
|------|------------------|---------|
| `packages/maestra-ai/src/maestra_ai/core/taste.py` | TasteProfile — perfis consolidados | Adiciona `_validate_restore_payload` |
| `packages/maestra-ai/src/maestra_ai/core/ratelimit.py` | Rate limit + circuit breaker persistentes | Adiciona `reset_breaker()` |
| `packages/maestra-ai/src/maestra_ai/cli/doctor.py` | CLI `maestra doctor` | Adiciona `--reset-breaker` |
| `packages/maestra-ai/src/maestra_ai/core/snapshot.py` | Snapshots | Aperta `_SNAP_ID_RE` |
| `packages/maestra-ai/src/maestra_ai/core/security.py` | Redação de secrets | Refatora `_SECRET_RE` com lista de prefixos conhecidos |
| `packages/maestra-ai/src/maestra_ai/core/client.py` | SpotifyController | Substitui `pgrep` por helper portável |
| `packages/maestra-ai/src/maestra_ai/core/process.py` | **NOVO** — helper de sondagem de processo | `is_spotify_running()` cross-platform |
| `packages/maestra-ai/src/maestra_ai/core/audit.py` | Audit log | Adiciona trigger de rotação por tamanho |
| `packages/maestra-ai/tests/unit/test_taste_restore.py` | **NOVO** | Testes de validação do restore |
| `packages/maestra-ai/tests/unit/test_ratelimit_breaker_reset.py` | **NOVO** | Testes de reset do breaker |
| `packages/maestra-ai/tests/unit/test_cli_doctor.py` | Existente | Adiciona cenários `--reset-breaker` |
| `packages/maestra-ai/tests/unit/test_snapshot.py` | Existente | Adiciona testes de regex estrita |
| `packages/maestra-ai/tests/unit/test_security.py` | Existente | Adiciona casos de falso-positivo |
| `packages/maestra-ai/tests/unit/test_process.py` | **NOVO** | Testes do helper de processo |
| `packages/maestra-ai/tests/unit/test_audit_rotation.py` | **NOVO** | Testes de rotação por tamanho |

---

## Task Ordering

Ordem escolhida pelo isolamento (menos acoplamento primeiro, maior integração no fim):

1. Task 1 — HIGH-4 SNAP_ID regex (contido, sem dependência)
2. Task 2 — HIGH-2 taste.restore validação
3. Task 3 — MEDIUM-1 _SECRET_RE refatoração
4. Task 4 — MEDIUM-3 rotação do audit por tamanho
5. Task 5 — MEDIUM-2 pgrep portável (extrai helper + usa em client.py)
6. Task 6 — HIGH-3 circuit breaker reset (core + CLI)
7. Task 7 — Release: bump 0.4.5 → 0.5.0, CHANGELOG, tag

---

### Task 1: HIGH-4 — SNAP_ID regex estrita

**Files:**
- Modify: `packages/maestra-ai/src/maestra_ai/core/snapshot.py:15`
- Test: `packages/maestra-ai/tests/unit/test_snapshot.py` (existente)

**Contexto:** Formato real de ID hoje é `%Y-%m-%d-%H%M%S-%f-<operation>`, ex: `2026-04-19-142530-000000-safety-before-rollback`. Regex atual `^[\w:\-]+$` aceita `foo`, `bar`, `../etc/passwd_disfarçado`. Apertar para o formato produzido.

- [ ] **Step 1: Escrever teste para aceitar formato canônico**

Em `packages/maestra-ai/tests/unit/test_snapshot.py`, adicionar:

```python
def test_snap_id_regex_aceita_formato_canonico():
    from maestra_ai.core.snapshot import _SNAP_ID_RE
    assert _SNAP_ID_RE.match("2026-04-19-142530-000000-safety-before-rollback")
    assert _SNAP_ID_RE.match("2026-04-19-142530-000000-pre_prune")
    assert _SNAP_ID_RE.match("2026-01-01-000000-000000-op")


def test_snap_id_regex_rejeita_formatos_invalidos():
    from maestra_ai.core.snapshot import _SNAP_ID_RE
    # ausência de partes
    assert not _SNAP_ID_RE.match("foo")
    assert not _SNAP_ID_RE.match("2026-04-19-safety")
    # caracteres fora do whitelist
    assert not _SNAP_ID_RE.match("2026-04-19-142530-000000-op/etc")
    assert not _SNAP_ID_RE.match("../etc/passwd")
    # espaços
    assert not _SNAP_ID_RE.match("2026-04-19-142530-000000-op with space")
```

- [ ] **Step 2: Rodar os testes novos — esperado FALHAR**

Run: `uv run pytest packages/maestra-ai/tests/unit/test_snapshot.py::test_snap_id_regex_rejeita_formatos_invalidos -v`
Expected: FAIL (regex atual aceita `"foo"`).

- [ ] **Step 3: Apertar regex em `snapshot.py:15`**

Substituir:

```python
_SNAP_ID_RE = re.compile(r"^[\w:\-]+$")
```

Por:

```python
_SNAP_ID_RE = re.compile(
    r"^\d{4}-\d{2}-\d{2}-\d{6}-\d{6}-[a-z][a-z0-9_\-]*$"
)
```

Nota: operation pode conter `_` e `-` mas deve começar com letra minúscula (convenção dos callers atuais — vide `snapshot.create(operation=...)` usos).

- [ ] **Step 4: Rodar suite inteira — esperado passar**

Run: `uv run pytest packages/maestra-ai/tests/unit/test_snapshot.py -v`
Expected: PASS. Se algum teste existente quebrar (ex: usa ID `"foo"` hardcoded), corrige no teste para usar um ID canônico válido (não afrouxa regex).

Run: `uv run pytest packages/maestra-ai/tests -q`
Expected: todos passam.

- [ ] **Step 5: Commit**

```bash
git add packages/maestra-ai/src/maestra_ai/core/snapshot.py \
        packages/maestra-ai/tests/unit/test_snapshot.py
git commit -m "fix(snapshot): regex estrita de SNAP_ID (HIGH-4)"
```

---

### Task 2: HIGH-2 — Validação em `taste.restore`

**Files:**
- Modify: `packages/maestra-ai/src/maestra_ai/core/taste.py:97`
- Modify: `packages/maestra-ai/src/maestra_ai/core/errors.py` (se `ValidationError` não existir)
- Test: `packages/maestra-ai/tests/unit/test_taste_restore.py` (**novo**)

**Contexto:** `taste.restore(data)` sobrescreve perfil sem validar. Um JSON corrompido (ou maldoso, vindo de snapshot adulterado) corrompe o perfil silenciosamente.

**Schema esperado** (derivado de `_migrate` e uso em `TasteProfile`):
- Tipo: `dict`
- Chaves aceitas: `global` (dict), `tracks` (dict), `success_rates` (dict), `context_tokens` (dict), `version` (int ou ausente)
- Valores em `tracks`: dict com `name` (str|None), `artist` (str|None), `weight` (int|float), e opcionais `positive`, `negative`

- [ ] **Step 1: Escrever testes de validação**

Criar `packages/maestra-ai/tests/unit/test_taste_restore.py`:

```python
"""HIGH-2: taste.restore valida schema antes de sobrescrever."""
from __future__ import annotations

import pytest

from maestra_ai.core.errors import ValidationError
from maestra_ai.core.taste import TasteProfile


def test_restore_aceita_payload_valido(tmp_path):
    tp = TasteProfile(path=str(tmp_path / "taste.json"))
    payload = {
        "global": {"positive_count": 5},
        "tracks": {
            "spotify:track:abc": {
                "name": "Foo",
                "artist": "Bar",
                "weight": 3,
            }
        },
        "success_rates": {},
        "context_tokens": {},
    }
    tp.restore(payload)
    assert tp.data["tracks"]["spotify:track:abc"]["weight"] == 3


def test_restore_rejeita_tipo_errado(tmp_path):
    tp = TasteProfile(path=str(tmp_path / "taste.json"))
    with pytest.raises(ValidationError):
        tp.restore("não é um dict")


def test_restore_rejeita_tracks_nao_dict(tmp_path):
    tp = TasteProfile(path=str(tmp_path / "taste.json"))
    with pytest.raises(ValidationError):
        tp.restore({"tracks": [1, 2, 3]})


def test_restore_rejeita_track_sem_estrutura_esperada(tmp_path):
    tp = TasteProfile(path=str(tmp_path / "taste.json"))
    with pytest.raises(ValidationError):
        tp.restore({"tracks": {"spotify:track:abc": "should be dict"}})


def test_restore_rejeita_weight_nao_numerico(tmp_path):
    tp = TasteProfile(path=str(tmp_path / "taste.json"))
    with pytest.raises(ValidationError):
        tp.restore({
            "tracks": {
                "spotify:track:abc": {"weight": "three"}
            }
        })


def test_restore_nao_corrompe_perfil_em_falha(tmp_path):
    """Se validação falhar, perfil em memória E em disco devem permanecer intactos."""
    path = tmp_path / "taste.json"
    tp = TasteProfile(path=str(path))
    tp.record_global_positive("spotify:track:original", name="Original", weight=5)
    original_data = dict(tp.data)

    with pytest.raises(ValidationError):
        tp.restore("payload inválido")

    # Memória preservada
    assert tp.data == original_data
    # Disco não foi tocado com lixo
    if path.exists():
        import json
        on_disk = json.loads(path.read_text())
        assert "spotify:track:original" in on_disk.get("tracks", {})
```

- [ ] **Step 2: Rodar — esperado FALHAR**

Run: `uv run pytest packages/maestra-ai/tests/unit/test_taste_restore.py -v`
Expected: FAIL em todos os testes "rejeita" (restore aceita qualquer coisa hoje).

- [ ] **Step 3: Adicionar `ValidationError` em errors.py (se ausente)**

Verificar: `grep -n "ValidationError" packages/maestra-ai/src/maestra_ai/core/errors.py`. Se não existe, adicionar como subclasse de `UserError` (ou `MaestraError` direto — seguir o padrão das demais: `UserError`, `StorageError`, `NotFoundError` em `errors.py`).

```python
class ValidationError(UserError):
    """Payload não passou em validação de schema."""
```

- [ ] **Step 4: Implementar `_validate_restore_payload` em taste.py**

Adicionar antes de `def restore`:

```python
def _validate_restore_payload(data):
    """HIGH-2: valida estrutura mínima antes de sobrescrever perfil.

    Rejeita: não-dict, tracks não-dict, entradas de track sem estrutura
    dict, weight não-numérico.
    """
    from maestra_ai.core.errors import ValidationError

    if not isinstance(data, dict):
        raise ValidationError(
            f"taste.restore: payload deve ser dict, veio {type(data).__name__}",
        )
    tracks = data.get("tracks")
    if tracks is not None and not isinstance(tracks, dict):
        raise ValidationError(
            f"taste.restore: 'tracks' deve ser dict, veio {type(tracks).__name__}",
        )
    if isinstance(tracks, dict):
        for uri, entry in tracks.items():
            if not isinstance(entry, dict):
                raise ValidationError(
                    f"taste.restore: track {uri!r} deve ser dict",
                )
            if "weight" in entry and not isinstance(entry["weight"], (int, float)):
                raise ValidationError(
                    f"taste.restore: track {uri!r} 'weight' deve ser numérico",
                )
    for key in ("global", "success_rates", "context_tokens"):
        if key in data and not isinstance(data[key], dict):
            raise ValidationError(
                f"taste.restore: {key!r} deve ser dict",
            )
```

Modificar `restore` (linha 97) — chamar validador ANTES de qualquer mutação:

```python
def restore(self, data):
    """Sobrescreve perfil com `data` sem merge (uso: rollback).

    HIGH-2: valida schema antes; se inválido, perfil não é tocado.
    """
    _validate_restore_payload(data)
    self.data = self._migrate(data)
    os.makedirs(os.path.dirname(self.path) or ".", exist_ok=True)
    lock_path = f"{self.path}.lock"
    with open(lock_path, "w", encoding="utf-8") as lock:
        fcntl.flock(lock, fcntl.LOCK_EX)
        tmp_path = f"{self.path}.tmp"
        with open(tmp_path, "w", encoding="utf-8") as f:
            json.dump(self.data, f, indent=2, ensure_ascii=False)
            f.write("\n")
        os.replace(tmp_path, self.path)
```

- [ ] **Step 5: Rodar — esperado passar**

Run: `uv run pytest packages/maestra-ai/tests/unit/test_taste_restore.py -v`
Expected: todos PASS.

Run: `uv run pytest packages/maestra-ai/tests -q`
Expected: 0 regressões.

- [ ] **Step 6: Commit**

```bash
git add packages/maestra-ai/src/maestra_ai/core/taste.py \
        packages/maestra-ai/src/maestra_ai/core/errors.py \
        packages/maestra-ai/tests/unit/test_taste_restore.py
git commit -m "fix(taste): valida schema em restore (HIGH-2)"
```

---

### Task 3: MEDIUM-1 — Redação contextual de secrets

**Files:**
- Modify: `packages/maestra-ai/src/maestra_ai/core/security.py`
- Test: `packages/maestra-ai/tests/unit/test_security.py` (existente)

**Contexto:** Regex atual `[A-Za-z0-9_\-]{32,}` mascara qualquer string longa: UUIDs de tracks, `spotify:track:...` URIs, IDs de playlist, nomes de álbuns com hash. Gera falsos positivos que poluem logs de debug.

**Estratégia:** Substituir redação cega por redação com contexto — só redacta em três casos:
1. Prefixo `Bearer ` seguido de token
2. Prefixo conhecido de key=value: `access_token=...`, `refresh_token=...`, `client_secret=...`, `api_key=...`
3. JWT-shape (3 segmentos base64url separados por `.`)

- [ ] **Step 1: Testes para preservar strings inocentes e manter redação de tokens reais**

Em `packages/maestra-ai/tests/unit/test_security.py`, adicionar:

```python
def test_redact_str_preserva_track_uri():
    from maestra_ai.core.security import redact_str
    s = "tocando spotify:track:1V2aEtKkJxLyJcxAz94nLY agora"
    # URI de track NÃO deve ser redatado
    assert "spotify:track:1V2aEtKkJxLyJcxAz94nLY" in redact_str(s)


def test_redact_str_preserva_playlist_id_em_contexto():
    from maestra_ai.core.security import redact_str
    s = "playlist 1V2aEtKkJxLyJcxAz94nLY sincronizada"
    out = redact_str(s)
    assert "1V2aEtKkJxLyJcxAz94nLY" in out


def test_redact_str_preserva_nomes_longos():
    from maestra_ai.core.security import redact_str
    s = "faixa: A Million Miles Away From Anywhere Anyone Has Ever Been Before"
    assert "REDACTED" not in redact_str(s)


def test_redact_str_redacta_bearer_token():
    from maestra_ai.core.security import redact_str
    s = "Authorization: Bearer BQC3abc123xyz987longtoken"
    assert "BQC3abc123xyz987longtoken" not in redact_str(s)
    assert "REDACTED" in redact_str(s)


def test_redact_str_redacta_key_value_conhecido():
    from maestra_ai.core.security import redact_str
    cases = [
        "access_token=BQC3abc123longvalue456",
        "refresh_token=AQD_xyz789longvalue123",
        "client_secret=abc123secret789longvalue",
        "api_key=pk_live_abc123longvalue456",
    ]
    for s in cases:
        out = redact_str(s)
        assert "REDACTED" in out, f"não redatou: {s!r}"


def test_redact_str_redacta_jwt():
    from maestra_ai.core.security import redact_str
    s = "token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjMifQ.abc123"
    assert "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9" not in redact_str(s)
```

- [ ] **Step 2: Rodar — esperado FALHAR nos "preserva"**

Run: `uv run pytest packages/maestra-ai/tests/unit/test_security.py -v`
Expected: os "preserva_*" falham (regex atual mata tudo >=32 chars).

- [ ] **Step 3: Refatorar `_SECRET_RE` em security.py**

Substituir:

```python
_SECRET_RE = re.compile(
    r"(Bearer\s+[A-Za-z0-9\-_\.]+|[A-Za-z0-9_\-]{32,})"
)
```

Por:

```python
# Redação contextual — só mascara quando houver pista de que é secret:
#  1. Bearer <token>
#  2. <known_key>=<value>   (access_token, refresh_token, client_secret, api_key)
#  3. JWT shape             (3 segmentos base64url separados por '.')
_KNOWN_SECRET_KEYS = (
    "access_token", "refresh_token", "client_secret",
    "api_key", "apikey", "authorization", "auth",
)
_SECRET_RE = re.compile(
    r"("
    r"Bearer\s+[A-Za-z0-9\-_\.]+"
    r"|(?:" + "|".join(_KNOWN_SECRET_KEYS) + r")\s*[:=]\s*[A-Za-z0-9\-_\.]+"
    r"|eyJ[A-Za-z0-9_\-]+\.eyJ[A-Za-z0-9_\-]+\.[A-Za-z0-9_\-]+"
    r")",
    re.IGNORECASE,
)
```

- [ ] **Step 4: Rodar — tudo passa**

Run: `uv run pytest packages/maestra-ai/tests/unit/test_security.py -v`
Expected: PASS.

Run: `uv run pytest packages/maestra-ai/tests -q`
Expected: 0 regressões. Se algum teste antigo assumia redação agressiva (ex: verifica `"REDACTED"` para um UUID qualquer), atualizar o teste para refletir o comportamento novo (mais correto).

- [ ] **Step 5: Commit**

```bash
git add packages/maestra-ai/src/maestra_ai/core/security.py \
        packages/maestra-ai/tests/unit/test_security.py
git commit -m "fix(security): redação contextual de secrets (MEDIUM-1)"
```

---

### Task 4: MEDIUM-3 — Rotação do audit por tamanho

**Files:**
- Modify: `packages/maestra-ai/src/maestra_ai/core/audit.py:66`
- Test: `packages/maestra-ai/tests/unit/test_audit_rotation.py` (**novo**)

**Contexto:** `_maybe_rotate` hoje só rota por idade (`_ACTIVE_DAYS`). Em uso intensivo via MCP, o log cresce >100MB em dias sem rotacionar.

**Estratégia:** Adicionar constante `_MAX_SIZE_BYTES = 10 * 1024 * 1024` (10MB). `_maybe_rotate` chama `_force_rotate` se arquivo excede tamanho OU idade.

- [ ] **Step 1: Testes de rotação por tamanho**

Criar `packages/maestra-ai/tests/unit/test_audit_rotation.py`:

```python
"""MEDIUM-3: rotação do audit.log por tamanho além de por idade."""
from __future__ import annotations


def test_rota_quando_excede_tamanho_maximo(monkeypatch, tmp_path):
    monkeypatch.setenv("MAESTRA_STATE_DIR", str(tmp_path))
    from maestra_ai.core import audit, storage

    # Grava muitos eventos até ultrapassar o limite.
    monkeypatch.setattr(audit, "_MAX_SIZE_BYTES", 1024)  # 1KB para ser rápido
    for i in range(100):
        audit.log(tool="test", args={"i": i}, result={"ok": True})

    # Deve existir pelo menos um arquivo arquivado .gz
    archives = list(storage.state_dir().glob("audit.*.jsonl.gz"))
    assert len(archives) >= 1, \
        f"esperava arquivo arquivado, veio: {list(storage.state_dir().iterdir())}"

    # E o audit.log ativo deve estar abaixo do limite
    active = storage.state_dir() / "audit.jsonl"
    if active.exists():
        assert active.stat().st_size <= 1024 * 2, \
            "audit ativo deveria ter sido rotacionado"


def test_nao_rota_abaixo_do_tamanho(monkeypatch, tmp_path):
    monkeypatch.setenv("MAESTRA_STATE_DIR", str(tmp_path))
    from maestra_ai.core import audit, storage

    monkeypatch.setattr(audit, "_MAX_SIZE_BYTES", 10 * 1024 * 1024)  # 10MB
    audit.log(tool="test", args={}, result={"ok": True})

    archives = list(storage.state_dir().glob("audit.*.jsonl.gz"))
    assert len(archives) == 0, "não deveria rotacionar para 1 evento pequeno"
```

**Nota:** se `MAESTRA_STATE_DIR` não for honrado por `storage.state_dir()`, ajuste para usar o mecanismo efetivo (`MAESTRA_DATA_DIR` ou similar — confirme via `grep "state_dir" packages/maestra-ai/src/maestra_ai/core/storage.py`).

- [ ] **Step 2: Rodar — esperado FALHAR**

Run: `uv run pytest packages/maestra-ai/tests/unit/test_audit_rotation.py -v`
Expected: FAIL (rotação por tamanho não existe).

- [ ] **Step 3: Implementar em audit.py**

Adicionar constante no topo das constantes existentes:

```python
_MAX_SIZE_BYTES = 10 * 1024 * 1024  # 10MB — dispara rotação mesmo antes da idade
```

Modificar `_maybe_rotate` (linha 66):

```python
def _maybe_rotate() -> None:
    """Chama rotate se arquivo tem tamanho ou idade que justifique."""
    p = _path_active()
    if not p.exists():
        return
    # Tamanho: checagem barata (stat), roda antes da idade
    try:
        if p.stat().st_size >= _MAX_SIZE_BYTES:
            _force_rotate()
            return
    except OSError:
        return
    age = (datetime.now(UTC) - datetime.fromtimestamp(p.stat().st_mtime, tz=UTC)).days
    if age >= _ACTIVE_DAYS:
        _force_rotate()
```

- [ ] **Step 4: Rodar — passa**

Run: `uv run pytest packages/maestra-ai/tests/unit/test_audit_rotation.py -v`
Expected: PASS.

Run: `uv run pytest packages/maestra-ai/tests -q`
Expected: 0 regressões.

- [ ] **Step 5: Commit**

```bash
git add packages/maestra-ai/src/maestra_ai/core/audit.py \
        packages/maestra-ai/tests/unit/test_audit_rotation.py
git commit -m "feat(audit): rotação por tamanho além de idade (MEDIUM-3)"
```

---

### Task 5: MEDIUM-2 — Sonda de processo portável

**Files:**
- Create: `packages/maestra-ai/src/maestra_ai/core/process.py`
- Modify: `packages/maestra-ai/src/maestra_ai/core/client.py:160`
- Test: `packages/maestra-ai/tests/unit/test_process.py` (**novo**)

**Contexto:** `subprocess.run(["pgrep", "-x", "spotify"])` em `client.py:160` é Linux-only (macOS tem mas semântica difere; Windows não tem). Abordagem: extrair helper `is_spotify_running()` que tenta `pgrep`, tem fallback para `tasklist` no Windows, e em último caso retorna `None` (desconhecido — chamador decide).

**Por que não `psutil`:** adicionaria dependência runtime nova. A checagem é só UX — se falhar, o API do Spotify já retorna erro claro.

- [ ] **Step 1: Testes do helper**

Criar `packages/maestra-ai/tests/unit/test_process.py`:

```python
"""MEDIUM-2: is_spotify_running portável (pgrep Linux/mac, tasklist Windows, fallback)."""
from __future__ import annotations

from unittest.mock import patch


def test_is_spotify_running_pgrep_encontra():
    """Em sistemas com pgrep e processo rodando, retorna True."""
    import subprocess
    from maestra_ai.core import process

    fake = subprocess.CompletedProcess(args=[], returncode=0, stdout=b"1234\n")
    with patch("maestra_ai.core.process.shutil.which", return_value="/usr/bin/pgrep"):
        with patch("maestra_ai.core.process.subprocess.run", return_value=fake):
            assert process.is_spotify_running() is True


def test_is_spotify_running_pgrep_nao_encontra():
    import subprocess
    from maestra_ai.core import process

    fake = subprocess.CompletedProcess(args=[], returncode=1, stdout=b"")
    with patch("maestra_ai.core.process.shutil.which", return_value="/usr/bin/pgrep"):
        with patch("maestra_ai.core.process.subprocess.run", return_value=fake):
            assert process.is_spotify_running() is False


def test_is_spotify_running_sem_pgrep_nem_tasklist_retorna_none():
    """Sem ferramentas disponíveis: retorna None (desconhecido, não False)."""
    from maestra_ai.core import process

    with patch("maestra_ai.core.process.shutil.which", return_value=None):
        assert process.is_spotify_running() is None


def test_is_spotify_running_tasklist_windows():
    import subprocess
    from maestra_ai.core import process

    def which_side(cmd):
        return r"C:\Windows\System32\tasklist.exe" if cmd == "tasklist" else None

    fake = subprocess.CompletedProcess(
        args=[], returncode=0,
        stdout=b'"Spotify.exe","1234","Console","1","100,000 K"\r\n',
    )
    with patch("maestra_ai.core.process.shutil.which", side_effect=which_side):
        with patch("maestra_ai.core.process.subprocess.run", return_value=fake):
            assert process.is_spotify_running() is True
```

- [ ] **Step 2: Rodar — esperado FALHAR**

Run: `uv run pytest packages/maestra-ai/tests/unit/test_process.py -v`
Expected: ModuleNotFoundError — arquivo não existe ainda.

- [ ] **Step 3: Criar helper `core/process.py`**

```python
"""Sonda de processo portável — MEDIUM-2.

Checa se o aplicativo Spotify está rodando. Retorna:
- True: processo encontrado
- False: processo não encontrado (ferramenta disponível e retornou vazio)
- None: não foi possível determinar (ferramenta ausente no sistema)

Caller decide como tratar None (tipicamente: prosseguir sem pré-check
e deixar a API do Spotify retornar o erro).
"""
from __future__ import annotations

import shutil
import subprocess


def is_spotify_running() -> bool | None:
    # Unix-like: pgrep
    pgrep = shutil.which("pgrep")
    if pgrep:
        try:
            result = subprocess.run(
                [pgrep, "-x", "spotify"],
                capture_output=True, timeout=2,
            )
            return result.returncode == 0
        except (subprocess.TimeoutExpired, OSError):
            return None

    # Windows: tasklist
    tasklist = shutil.which("tasklist")
    if tasklist:
        try:
            result = subprocess.run(
                [tasklist, "/FI", "IMAGENAME eq Spotify.exe", "/FO", "CSV", "/NH"],
                capture_output=True, timeout=2,
            )
            if result.returncode != 0:
                return None
            # tasklist retorna "INFO: No tasks..." no stdout se vazio
            out = result.stdout.decode("utf-8", errors="replace").strip()
            return bool(out) and "No tasks" not in out and "Spotify.exe" in out
        except (subprocess.TimeoutExpired, OSError):
            return None

    return None
```

- [ ] **Step 4: Usar em `client.py:150-162`**

Substituir:

```python
# 1. Processo rodando?
result = subprocess.run(["pgrep", "-x", "spotify"], capture_output=True)
if result.returncode != 0:
    raise RuntimeError("Processo do Spotify não encontrado. Abra o Spotify primeiro.")
```

Por:

```python
# 1. Processo rodando? (pré-check; se inconclusivo, segue e deixa API falar)
from maestra_ai.core.process import is_spotify_running
running = is_spotify_running()
if running is False:
    raise RuntimeError("Processo do Spotify não encontrado. Abra o Spotify primeiro.")
# running is None: não dá pra checar neste SO — prossegue
```

Remover import `subprocess` de client.py se tornar-se não usado. Rodar ruff para confirmar.

- [ ] **Step 5: Rodar — passa**

Run: `uv run pytest packages/maestra-ai/tests/unit/test_process.py packages/maestra-ai/tests/unit/test_client.py -v`
Expected: PASS.

Run: `uv run pytest packages/maestra-ai/tests -q && uv run ruff check packages/`
Expected: 0 regressões, ruff clean.

- [ ] **Step 6: Commit**

```bash
git add packages/maestra-ai/src/maestra_ai/core/process.py \
        packages/maestra-ai/src/maestra_ai/core/client.py \
        packages/maestra-ai/tests/unit/test_process.py
git commit -m "feat(process): sonda Spotify portável (MEDIUM-2)"
```

---

### Task 6: HIGH-3 — Reset manual do circuit breaker

**Files:**
- Modify: `packages/maestra-ai/src/maestra_ai/core/ratelimit.py`
- Modify: `packages/maestra-ai/src/maestra_ai/cli/doctor.py`
- Test: `packages/maestra-ai/tests/unit/test_ratelimit_breaker_reset.py` (**novo**)
- Test: `packages/maestra-ai/tests/unit/test_cli_doctor.py` (existente)

**Contexto:** `PersistentCircuitBreaker` abre após N falhas consecutivas. Auto-fecha após cooldown. Não há jeito de forçar reset sem deletar manualmente o row do SQLite. Fix: função `reset_breaker()` no core + flag `--reset-breaker` no `maestra doctor`.

- [ ] **Step 1: Explorar ratelimit.py para entender estrutura**

Run: `grep -n "class\|def\|breaker\|circuit" packages/maestra-ai/src/maestra_ai/core/ratelimit.py`

Anotar: nome da classe do breaker, como ela acessa o SQLite, se há state em memória (`_state`, `_failures` etc.), e se já existe alguma função `_reset` interna.

- [ ] **Step 2: Testes da função de reset**

Criar `packages/maestra-ai/tests/unit/test_ratelimit_breaker_reset.py`:

```python
"""HIGH-3: reset manual do circuit breaker (core + CLI)."""
from __future__ import annotations


def test_reset_breaker_zera_estado_aberto(monkeypatch, tmp_path):
    """Breaker aberto deve ficar fechado após reset_breaker()."""
    monkeypatch.setenv("MAESTRA_DATA_DIR", str(tmp_path))
    from maestra_ai.core import ratelimit

    # Força abertura simulando falhas consecutivas
    breaker = ratelimit._get_breaker()
    # Aciona o equivalente de "N falhas" — exato número é detalhe interno;
    # chamamos record_failure repetidamente até allow() virar False.
    for _ in range(20):
        if hasattr(breaker, "record_failure"):
            breaker.record_failure()
        else:
            break
    # Se breaker é no-op em testes (fallback in-memory), teste degrada para smoke:
    was_open = not breaker.allow()

    ratelimit.reset_breaker()

    # Após reset, deve permitir de novo
    breaker_after = ratelimit._get_breaker()
    assert breaker_after.allow() is True, "reset deve reabrir o breaker"
    # E, se conseguimos abri-lo antes, confirma que a função de fato fez algo
    if was_open:
        assert breaker_after.allow() is True  # reiterando intenção


def test_reset_breaker_idempotente(monkeypatch, tmp_path):
    """Chamar reset em breaker já fechado é seguro (no-op)."""
    monkeypatch.setenv("MAESTRA_DATA_DIR", str(tmp_path))
    from maestra_ai.core import ratelimit

    ratelimit.reset_breaker()
    ratelimit.reset_breaker()
    assert ratelimit._get_breaker().allow() is True
```

E em `test_cli_doctor.py`:

```python
def test_doctor_reset_breaker_flag_invoca_reset(monkeypatch, tmp_path, capsys):
    monkeypatch.setenv("MAESTRA_DATA_DIR", str(tmp_path))
    from maestra_ai.cli import doctor as doctor_mod

    called = {"n": 0}
    def fake_reset():
        called["n"] += 1

    monkeypatch.setattr("maestra_ai.core.ratelimit.reset_breaker", fake_reset)

    # Simula `maestra doctor --reset-breaker`
    import argparse
    ns = argparse.Namespace(reset_breaker=True, json=False)
    doctor_mod.cmd_doctor(ns)

    captured = capsys.readouterr()
    assert called["n"] == 1
    assert "breaker" in captured.out.lower() or "reset" in captured.out.lower()
```

- [ ] **Step 3: Rodar — esperado FALHAR**

Run: `uv run pytest packages/maestra-ai/tests/unit/test_ratelimit_breaker_reset.py -v`
Expected: FAIL — `reset_breaker` não existe.

- [ ] **Step 4: Implementar `reset_breaker()` em ratelimit.py**

No topo do módulo (após definição do breaker), adicionar:

```python
def reset_breaker() -> None:
    """HIGH-3: força fechamento do circuit breaker.

    Útil quando operador corrigiu a causa-raiz de falhas (credencial
    trocada, rede restabelecida) e não quer esperar o cooldown.
    """
    breaker = _get_breaker()
    # Estratégia: zerar estado persistido + limpar cache em memória.
    if hasattr(breaker, "reset"):
        breaker.reset()
    else:
        # Fallback: recria o SQLite row
        import sqlite3
        from maestra_ai.core import storage
        db_path = storage.data_dir() / "ratelimit.db"
        if db_path.exists():
            conn = sqlite3.connect(str(db_path))
            try:
                conn.execute("DELETE FROM breaker_state")
                conn.commit()
            except sqlite3.OperationalError:
                pass  # tabela pode não existir ainda
            finally:
                conn.close()
    # Limpa cache global do módulo para forçar re-leitura
    global _breaker_cache
    _breaker_cache = None
```

**Nota:** ajustar nome da variável cacheada (`_breaker_cache` ou equivalente) conforme o módulo. Se `PersistentCircuitBreaker` tiver método `reset()`, preferir ele. Caso contrário, implementar o método `reset()` na classe (mais limpo que o DELETE direto).

- [ ] **Step 5: Adicionar flag em `cli/doctor.py`**

Localizar parser do `doctor`:

```python
parser.add_argument(
    "--reset-breaker",
    action="store_true",
    help="Zera o circuit breaker persistente (fecha, permite nova tentativa).",
)
```

No handler `cmd_doctor`, antes do fluxo de diagnósticos:

```python
if getattr(args, "reset_breaker", False):
    from maestra_ai.core.ratelimit import reset_breaker
    reset_breaker()
    print("[OK] Circuit breaker resetado.")
    return 0
```

- [ ] **Step 6: Rodar — passa**

Run: `uv run pytest packages/maestra-ai/tests/unit/test_ratelimit_breaker_reset.py packages/maestra-ai/tests/unit/test_cli_doctor.py -v`
Expected: PASS.

Run: `uv run pytest packages/maestra-ai/tests -q && uv run ruff check packages/`
Expected: 0 regressões, ruff clean.

- [ ] **Step 7: Commit**

```bash
git add packages/maestra-ai/src/maestra_ai/core/ratelimit.py \
        packages/maestra-ai/src/maestra_ai/cli/doctor.py \
        packages/maestra-ai/tests/unit/test_ratelimit_breaker_reset.py \
        packages/maestra-ai/tests/unit/test_cli_doctor.py
git commit -m "feat(doctor): --reset-breaker para fechar breaker manualmente (HIGH-3)"
```

---

### Task 7: Release v0.5.0

**Files:**
- Modify: `packages/maestra-ai/pyproject.toml:3`
- Modify: `packages/maestra-mcp/pyproject.toml:3`
- Modify: `CHANGELOG.md`

- [ ] **Step 1: Bump version**

Substituir `version = "0.4.5"` por `version = "0.5.0"` em ambos `pyproject.toml`.

- [ ] **Step 2: CHANGELOG**

Adicionar entrada logo após `## [Unreleased]`:

```markdown
## [0.5.0] - 2026-04-19

Polimento pós-v0.4.5. Fecha os 6 achados HIGH/MEDIUM remanescentes
do code review. Sem quebra de API pública.

### Fixed
- **`taste.restore` valida schema antes de sobrescrever** (HIGH-2):
  payload malformado levanta `ValidationError`; perfil em memória e
  em disco permanecem intactos.
- **`_SNAP_ID_RE` estrita** (HIGH-4): só aceita formato canônico
  `YYYY-MM-DD-HHMMSS-uuuuuu-<operation>`. Rejeita `../`, espaços,
  strings curtas.
- **Redação de secrets contextual** (MEDIUM-1): URIs de track,
  playlist IDs e nomes longos não são mais mascarados. Mantém
  redação de `Bearer ...`, `access_token=...`, `refresh_token=...`,
  `client_secret=...`, `api_key=...` e JWTs.
- **Sonda de processo Spotify portável** (MEDIUM-2): novo
  `core/process.py::is_spotify_running()` com fallback
  pgrep → tasklist → None. `client.ensure_active_device` trata
  None como "desconhecido, prossegue".

### Added
- **`maestra doctor --reset-breaker`** (HIGH-3): zera o circuit
  breaker persistente. Útil quando operador corrigiu a causa-raiz
  e não quer esperar o cooldown.
- **Rotação do audit.log por tamanho** (MEDIUM-3): `_MAX_SIZE_BYTES
  = 10MB` força rotação mesmo antes da idade (`_ACTIVE_DAYS`).

### Tests
- Novos módulos: `test_taste_restore`, `test_ratelimit_breaker_reset`,
  `test_process`, `test_audit_rotation`. Acréscimos em
  `test_snapshot`, `test_security`, `test_cli_doctor`.
- Suite total esperada: ~420 passed (maestra-ai + maestra-mcp).
```

- [ ] **Step 3: Suite final + ruff**

Run: `uv run pytest packages/maestra-ai/tests -q && uv run pytest packages/maestra-mcp/tests -q && uv run ruff check packages/`
Expected: ALL PASS, ruff clean.

- [ ] **Step 4: Commit + merge + tag + push**

```bash
git add packages/maestra-ai/pyproject.toml packages/maestra-mcp/pyproject.toml CHANGELOG.md
git commit -m "chore(release): bump 0.4.5 → 0.5.0 + CHANGELOG"
git checkout main
git merge --no-ff feat/v050-polish -m "Merge feat/v050: polish de 6 achados HIGH/MEDIUM (v0.5.0)"
git tag v0.5.0 -m "v0.5.0 — polimento de 6 achados HIGH/MEDIUM"
git push origin main
git push origin v0.5.0
git branch -d feat/v050-polish
```

---

## Self-Review

**Spec coverage:**
- HIGH-2 `taste.restore` validação → Task 2 ✅
- HIGH-3 circuit breaker reset → Task 6 ✅
- HIGH-4 SNAP_ID regex → Task 1 ✅
- MEDIUM-1 `_SECRET_RE` falsos positivos → Task 3 ✅
- MEDIUM-2 pgrep portável → Task 5 ✅
- MEDIUM-3 audit rotação por tamanho → Task 4 ✅

**Placeholder scan:** nenhum "TBD", "TODO", "similar to", "add validation". Código real em todos os steps. Pontos onde a introspecção do código é necessária (nome real da classe do breaker, existência de `ValidationError`) estão marcados com comando `grep` explícito e instrução de "ajustar conforme encontrado" — isso é legítimo, não placeholder.

**Type consistency:** `ValidationError` em Task 2 usado consistentemente. `is_spotify_running()` retorna `bool | None` em Task 5, respeitado no caller. `reset_breaker()` em Task 6 é função livre, consistente com padrão de `_get_breaker()` já existente.

**Escopo:** 6 tarefas independentes + release. Cada uma é commit atômico com TDD. Total estimado: ~25 testes novos, ~200 LOC de produção, ~350 LOC de teste.

---

## Execution Handoff

Plano salvo em `docs/superpowers/plans/2026-04-19-v050-polish.md`. Duas opções:

**1. Subagent-Driven (recomendado para 7 tasks)** — despacho subagente fresco por task, review entre tasks, isolamento de contexto.

**2. Inline execution** — execução em sessão contínua com checkpoints.

Qual preferes?
