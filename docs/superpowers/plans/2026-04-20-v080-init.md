# v0.8.0-alpha.0 — `maestra init` Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** introduzir `maestra init` como front door unificado (wizard guiado + modo `--auto` scripted), com linguagem sem jargão, menu por estado e retry loop caloroso. Depreca `maestra onboard` em v0.8, removido em v0.9.

**Architecture:** novo módulo `core/init.py` com state machine linear (detecta estado A/A2/B/C, renderiza menu, despacha para fluxo). Fluxos reutilizam helpers existentes de `core/onboard.py` e `core/auth.py` — `init.py` é orquestração + narrativa. CLI entry em `cli/init.py`.

**Tech Stack:** Python 3.11+, rich (prompt/panels), keyring, spotipy, jsonschema. Arquivos novos: `core/init.py`, `cli/init.py`, `src/maestra_ai/docs/topics/init.md`, `tests/unit/test_init.py`, `tests/integration/test_init_flow.py`. Modificados: `cli/__init__.py` (adicionar subcmd), `cli/onboard.py` (deprecation warning), `src/maestra_ai/docs/topics/onboarding.md` (banner), `pyproject.toml` (bump).

**Base:** commit `5decf82` (v0.7.0-alpha.1 + pytest rootdir fix). Suíte atual: 564 testes verdes.

**Referência:** `docs/superpowers/specs/2026-04-20-v080-init-design.md`.

---

## Decisões globais

- **TypedDicts** em `core/init_types.py` (seguindo padrão de `onboard_types.py`).
- **State machine** implementada como despacho explícito (`if state == "A": ...`), não classes hierárquicas — simplicidade.
- **Retry loop** é genérico (`_retry_loop(fn, error_classifier, hints)`), compartilhado entre as etapas. Não acopla a erros específicos.
- **Testes unitários** patcham `input()`/`rich.prompt.Prompt.ask`/`webbrowser.open` via monkeypatch. Integração usa `subprocess` + fixtures de rede mockadas.
- **Mensagens narrativas** ficam em constantes no topo de `core/init.py` (fácil de revisar/traduzir depois).

---

### Task 1: Scaffolding + state detection

**Files:**
- Create: `packages/maestra-ai/src/maestra_ai/core/init_types.py`
- Create: `packages/maestra-ai/src/maestra_ai/core/init.py`
- Create: `packages/maestra-ai/tests/unit/test_init.py`

- [ ] **Step 1: TypedDicts**

Crie `core/init_types.py`:

```python
"""Tipos do wizard `maestra init`."""
from __future__ import annotations
from typing import Literal, TypedDict


InitState = Literal["A", "A2", "B", "C"]


class InitReport(TypedDict):
    """Resultado de uma execução de init."""
    state_before: InitState
    action: Literal[
        "start_from_scratch",
        "resume_oauth",
        "initial_analysis",
        "update_recent_mood",
        "update_full",
        "reset_partial",
        "reset_full",
        "exit",
    ]
    playlist_id: str | None
    taste_profile_updated: bool
    rationale_path: str | None
    signals: dict | None
    suggestions: list[str]
    warnings: list[str]
```

- [ ] **Step 2: Failing test — `detect_state()` cobertura exaustiva**

Em `tests/unit/test_init.py`:

```python
"""Testes de `core/init.py`."""
from __future__ import annotations
import json
import pytest
from pathlib import Path


class TestDetectState:
    """Cobre as 4 combinações legítimas + 3 inconsistentes."""

    def test_empty_is_A(self, tmp_path, monkeypatch):
        from maestra_ai.core import init, storage
        monkeypatch.setattr(storage, "config_dir", lambda: tmp_path / "cfg")
        monkeypatch.setattr(storage, "data_dir", lambda: tmp_path / "data")
        monkeypatch.setattr(storage, "state_dir", lambda: tmp_path / "state")
        monkeypatch.setattr(init, "_has_token", lambda: False)
        assert init.detect_state() == "A"

    def test_config_only_is_A2(self, tmp_path, monkeypatch):
        from maestra_ai.core import init, storage
        cfg_dir = tmp_path / "cfg"
        cfg_dir.mkdir()
        (cfg_dir / "config.json").write_text(json.dumps({
            "client_id": "x", "client_secret": "y",
            "redirect_uri": "https://example.com/callback",
        }))
        monkeypatch.setattr(storage, "config_dir", lambda: cfg_dir)
        monkeypatch.setattr(storage, "data_dir", lambda: tmp_path / "data")
        monkeypatch.setattr(storage, "state_dir", lambda: tmp_path / "state")
        monkeypatch.setattr(init, "_has_token", lambda: False)
        assert init.detect_state() == "A2"

    def test_connected_no_taste_is_B(self, tmp_path, monkeypatch):
        from maestra_ai.core import init, storage
        cfg_dir = tmp_path / "cfg"
        cfg_dir.mkdir()
        (cfg_dir / "config.json").write_text(json.dumps({
            "client_id": "x", "client_secret": "y",
            "redirect_uri": "https://example.com/callback",
        }))
        monkeypatch.setattr(storage, "config_dir", lambda: cfg_dir)
        monkeypatch.setattr(storage, "data_dir", lambda: tmp_path / "data")
        monkeypatch.setattr(storage, "state_dir", lambda: tmp_path / "state")
        monkeypatch.setattr(init, "_has_token", lambda: True)
        assert init.detect_state() == "B"

    def test_everything_present_is_C(self, tmp_path, monkeypatch):
        from maestra_ai.core import init, storage
        cfg_dir = tmp_path / "cfg"; cfg_dir.mkdir()
        data_dir = tmp_path / "data"; data_dir.mkdir()
        (cfg_dir / "config.json").write_text(json.dumps({
            "client_id": "x", "client_secret": "y",
            "redirect_uri": "https://example.com/callback",
        }))
        (data_dir / "taste_profile.json").write_text(json.dumps({
            "global_signal": {"track_a": {"weight": 3.0}},
        }))
        monkeypatch.setattr(storage, "config_dir", lambda: cfg_dir)
        monkeypatch.setattr(storage, "data_dir", lambda: data_dir)
        monkeypatch.setattr(storage, "state_dir", lambda: tmp_path / "state")
        monkeypatch.setattr(init, "_has_token", lambda: True)
        assert init.detect_state() == "C"

    def test_token_without_config_treated_as_A(self, tmp_path, monkeypatch):
        """Token órfão sem config = inconsistente, volta pra A + aviso."""
        from maestra_ai.core import init, storage
        monkeypatch.setattr(storage, "config_dir", lambda: tmp_path / "cfg")
        monkeypatch.setattr(storage, "data_dir", lambda: tmp_path / "data")
        monkeypatch.setattr(storage, "state_dir", lambda: tmp_path / "state")
        monkeypatch.setattr(init, "_has_token", lambda: True)
        assert init.detect_state() == "A"

    def test_taste_without_token_treated_as_A(self, tmp_path, monkeypatch):
        """Taste órfão sem token = inconsistente, volta pra A + aviso."""
        from maestra_ai.core import init, storage
        data_dir = tmp_path / "data"; data_dir.mkdir()
        (data_dir / "taste_profile.json").write_text(json.dumps({
            "global_signal": {"x": {"weight": 1.0}},
        }))
        monkeypatch.setattr(storage, "config_dir", lambda: tmp_path / "cfg")
        monkeypatch.setattr(storage, "data_dir", lambda: data_dir)
        monkeypatch.setattr(storage, "state_dir", lambda: tmp_path / "state")
        monkeypatch.setattr(init, "_has_token", lambda: False)
        assert init.detect_state() == "A"

    def test_taste_profile_empty_global_signal_is_B(self, tmp_path, monkeypatch):
        """taste_profile existe mas global_signal vazio = ainda B."""
        from maestra_ai.core import init, storage
        cfg_dir = tmp_path / "cfg"; cfg_dir.mkdir()
        data_dir = tmp_path / "data"; data_dir.mkdir()
        (cfg_dir / "config.json").write_text(json.dumps({
            "client_id": "x", "client_secret": "y",
            "redirect_uri": "https://example.com/callback",
        }))
        (data_dir / "taste_profile.json").write_text(json.dumps({
            "global_signal": {},
        }))
        monkeypatch.setattr(storage, "config_dir", lambda: cfg_dir)
        monkeypatch.setattr(storage, "data_dir", lambda: data_dir)
        monkeypatch.setattr(storage, "state_dir", lambda: tmp_path / "state")
        monkeypatch.setattr(init, "_has_token", lambda: True)
        assert init.detect_state() == "B"
```

- [ ] **Step 3: Verify RED**

Run: `uv run pytest packages/maestra-ai/tests/unit/test_init.py -v`
Expected: 7 falhas (módulo `init` não existe).

- [ ] **Step 4: Implementar `detect_state` em `core/init.py`**

```python
"""Wizard unificado `maestra init`.

Detecta estado (A/A2/B/C), apresenta menu contextual, executa o fluxo
escolhido. Delega I/O externo para `core.auth` e `core.onboard`.
"""
from __future__ import annotations

import json
from typing import Literal

from maestra_ai.core import storage
from maestra_ai.core.init_types import InitState


def _has_token() -> bool:
    """True se há refresh_token persistido (keyring ou fallback)."""
    from maestra_ai.core.token_store import default_token_store
    try:
        tok = default_token_store().load()
        return bool(tok)
    except Exception:
        return False


def _has_config() -> bool:
    """True se config.json tem client_id e client_secret não-vazios."""
    path = storage.config_dir() / "config.json"
    if not path.exists():
        return False
    try:
        cfg = json.loads(path.read_text(encoding="utf-8"))
    except (OSError, json.JSONDecodeError):
        return False
    return bool(cfg.get("client_id")) and bool(cfg.get("client_secret"))


def _has_taste() -> bool:
    """True se taste_profile tem global_signal não-vazio."""
    path = storage.data_dir() / "taste_profile.json"
    if not path.exists():
        return False
    try:
        profile = json.loads(path.read_text(encoding="utf-8"))
    except (OSError, json.JSONDecodeError):
        return False
    return bool(profile.get("global_signal"))


def detect_state() -> InitState:
    """Retorna o estado atual do setup da Maestra.

    Estados inconsistentes (taste sem token, token sem config) voltam pra A
    — o chamador pode consultar o helper privado pra avisar o usuário.
    """
    has_config = _has_config()
    has_token = _has_token()
    has_taste = _has_taste()

    if has_config and has_token and has_taste:
        return "C"
    if has_config and has_token:
        return "B"
    if has_config and not has_token:
        return "A2"
    # Combinações inconsistentes caem em A
    return "A"
```

- [ ] **Step 5: Verify GREEN**

Run: `uv run pytest packages/maestra-ai/tests/unit/test_init.py -v`
Expected: 7 passed.

- [ ] **Step 6: Commit**

```bash
git add packages/maestra-ai/src/maestra_ai/core/init.py \
        packages/maestra-ai/src/maestra_ai/core/init_types.py \
        packages/maestra-ai/tests/unit/test_init.py
git commit -m "feat(init): scaffolding + detect_state (4 estados)"
```

---

### Task 2: Menu rendering por estado

**Files:**
- Modify: `packages/maestra-ai/src/maestra_ai/core/init.py`
- Modify: `packages/maestra-ai/tests/unit/test_init.py`

- [ ] **Step 1: RED — testes de rendering**

```python
class TestMenuRendering:
    def test_menu_A_mostra_saudacao_e_duas_opcoes(self, capsys):
        from maestra_ai.core import init
        init.render_menu("A")
        out = capsys.readouterr().out
        assert "Olá" in out
        assert "[1]" in out and "Começar agora" in out
        assert "[2]" in out and "Sair" in out

    def test_menu_A2_mostra_app_configurada(self, capsys):
        from maestra_ai.core import init
        init.render_menu("A2")
        out = capsys.readouterr().out
        assert "app Spotify já está configurada" in out or "app já está configurad" in out
        assert "[1]" in out and "autorizar" in out.lower()
        assert "[2]" in out and "Recomeçar" in out
        assert "[3]" in out and "Sair" in out

    def test_menu_B_mostra_conta_conectada(self, capsys):
        from maestra_ai.core import init
        init.render_menu("B")
        out = capsys.readouterr().out
        assert "conta Spotify já está conectada" in out
        assert "analisar preferências" in out
        assert "[3]" in out

    def test_menu_C_mostra_tudo_pronto(self, capsys):
        from maestra_ai.core import init
        init.render_menu("C")
        out = capsys.readouterr().out
        assert "Tudo pronto" in out
        assert "Atualizar preferências" in out
        assert "[2]" in out and "Recomeçar" in out

    def test_render_update_submenu(self, capsys):
        from maestra_ai.core import init
        init.render_update_submenu()
        out = capsys.readouterr().out
        assert "mood recente" in out.lower()
        assert "Tudo" in out
        assert "Voltar" in out
```

- [ ] **Step 2: Verify RED**

Run: `uv run pytest packages/maestra-ai/tests/unit/test_init.py::TestMenuRendering -v`
Expected: 5 fail (funções não existem).

- [ ] **Step 3: Implementar rendering**

Acrescentar em `core/init.py`:

```python
from rich.console import Console
from rich.panel import Panel

_console = Console()


_MENU_MESSAGES: dict[InitState, tuple[str, list[str]]] = {
    "A": (
        "Olá! Vamos configurar sua Maestra.",
        ["[1] Começar agora", "[2] Sair"],
    ),
    "A2": (
        "Sua app Spotify já está configurada. Só falta autorizar o acesso.",
        [
            "[1] Continuar — autorizar e analisar preferências",
            "[2] Recomeçar — apagar config e começar de novo",
            "[3] Sair",
        ],
    ),
    "B": (
        "Sua conta Spotify já está conectada. Só falta analisar suas preferências "
        "musicais para eu poder sugerir contextos.",
        [
            "[1] Continuar — analisar preferências agora",
            "[2] Recomeçar — apagar conexão e começar de novo",
            "[3] Sair",
        ],
    ),
    "C": (
        "Tudo pronto por aqui! O que você quer fazer?",
        [
            "[1] Atualizar preferências — re-analisar seu histórico recente",
            "[2] Recomeçar — apagar tudo e refazer",
            "[3] Sair",
        ],
    ),
}


def render_menu(state: InitState) -> None:
    """Imprime o menu apropriado para o estado."""
    header, options = _MENU_MESSAGES[state]
    body = header + "\n\n" + "\n".join(f"  {o}" for o in options)
    _console.print(Panel(body, border_style="cyan", padding=(1, 2)))


def render_update_submenu() -> None:
    """Sub-menu de C→[1]."""
    body = (
        "O que você quer atualizar?\n\n"
        "  [1] Só mood recente (últimas 4 semanas + histórico recente)\n"
        "  [2] Tudo (pode demorar mais)\n"
        "  [3] Voltar"
    )
    _console.print(Panel(body, border_style="cyan", padding=(1, 2)))
```

- [ ] **Step 4: Verify GREEN**

Run: `uv run pytest packages/maestra-ai/tests/unit/test_init.py::TestMenuRendering -v`
Expected: 5 passed.

- [ ] **Step 5: Commit**

```bash
git add packages/maestra-ai/src/maestra_ai/core/init.py \
        packages/maestra-ai/tests/unit/test_init.py
git commit -m "feat(init): render_menu por estado + sub-menu de update"
```

---

### Task 3: Retry loop helper

**Files:**
- Modify: `packages/maestra-ai/src/maestra_ai/core/init.py`
- Modify: `packages/maestra-ai/tests/unit/test_init.py`

- [ ] **Step 1: RED — retry 1x recupera, 3x esgota com smart hint**

```python
class TestRetryLoop:
    def test_sucesso_primeira_tentativa_nao_pergunta(self, capsys, monkeypatch):
        from maestra_ai.core import init
        calls = {"n": 0}
        def fn():
            calls["n"] += 1
            return "ok"

        result = init._retry_loop(
            fn,
            classifier=lambda e: "network",
            hints={"network": "Cheque sua conexão."},
        )
        assert result == "ok"
        assert calls["n"] == 1

    def test_recuperacao_na_segunda(self, capsys, monkeypatch):
        from maestra_ai.core import init
        calls = {"n": 0}
        def fn():
            calls["n"] += 1
            if calls["n"] == 1:
                raise ConnectionError("timeout")
            return "ok"

        # Mock da escolha [1] Tentar de novo
        monkeypatch.setattr(init, "_ask_retry", lambda: True)
        result = init._retry_loop(
            fn,
            classifier=lambda e: "network",
            hints={"network": "Cheque sua conexão."},
        )
        assert result == "ok"
        assert calls["n"] == 2

    def test_tres_falhas_mesmo_tipo_retorna_smart_hint(self, capsys, monkeypatch):
        from maestra_ai.core import init
        def fn():
            raise ConnectionError("fail")

        monkeypatch.setattr(init, "_ask_retry", lambda: True)
        # _ask_smart_exit retorna False = sair
        monkeypatch.setattr(init, "_ask_smart_exit", lambda *a, **k: False)

        with pytest.raises(init.UserAbort):
            init._retry_loop(
                fn,
                classifier=lambda e: "network",
                hints={"network": "Cheque sua conexão."},
            )
        out = capsys.readouterr().out
        assert "terceira tentativa" in out.lower() or "3" in out
```

- [ ] **Step 2: Verify RED**

Run: `uv run pytest packages/maestra-ai/tests/unit/test_init.py::TestRetryLoop -v`
Expected: 3 fail.

- [ ] **Step 3: Implementar helper**

Em `core/init.py`:

```python
from collections.abc import Callable
from typing import TypeVar

T = TypeVar("T")


class UserAbort(Exception):
    """Usuário escolheu sair voluntariamente."""


def _ask_retry() -> bool:
    """Pergunta '[1] Tentar de novo / [2] Sair'. Retorna True se tentar."""
    from rich.prompt import Prompt
    choice = Prompt.ask("Escolha", choices=["1", "2"], default="1")
    return choice == "1"


def _ask_smart_exit(error_kind: str, hint: str) -> bool:
    """Pergunta se abre link externo ou sai. Retorna True se quer tentar de novo."""
    from rich.prompt import Prompt
    _console.print(
        f"\n❌ Essa é a terceira tentativa com o mesmo erro ({error_kind}).\n\n"
        f"Parece que o problema precisa de ação externa. {hint}\n"
    )
    _console.print("  [1] Abrir no navegador e tentar de novo")
    _console.print("  [2] Sair e resolver depois (seu progresso foi salvo)")
    choice = Prompt.ask("Escolha", choices=["1", "2"], default="2")
    return choice == "1"


def _retry_loop(
    fn: Callable[[], T],
    *,
    classifier: Callable[[Exception], str],
    hints: dict[str, str],
    max_same_kind: int = 3,
    on_smart_exit_link: Callable[[str], None] | None = None,
) -> T:
    """Executa `fn()` com retry interativo até sucesso ou desistência.

    - Após N (default 3) falhas consecutivas do MESMO tipo, oferece smart exit
      com link e mensagem específica.
    - Usuário escolhendo sair levanta UserAbort.
    """
    same_kind_count = 0
    last_kind: str | None = None
    while True:
        try:
            return fn()
        except Exception as e:
            kind = classifier(e)
            hint = hints.get(kind, "Tente novamente em alguns segundos.")
            _console.print(f"\n❌ {e}\n\n{hint}\n")

            if kind == last_kind:
                same_kind_count += 1
            else:
                same_kind_count = 1
                last_kind = kind

            if same_kind_count >= max_same_kind:
                if _ask_smart_exit(kind, hint):
                    if on_smart_exit_link:
                        on_smart_exit_link(kind)
                    same_kind_count = 0  # dá mais uma chance após ação externa
                    continue
                raise UserAbort(f"Desistiu após {same_kind_count} tentativas ({kind})")

            if not _ask_retry():
                raise UserAbort("Usuário escolheu sair")
```

- [ ] **Step 4: Verify GREEN**

Run: `uv run pytest packages/maestra-ai/tests/unit/test_init.py::TestRetryLoop -v`
Expected: 3 passed.

- [ ] **Step 5: Commit**

```bash
git add packages/maestra-ai/src/maestra_ai/core/init.py \
        packages/maestra-ai/tests/unit/test_init.py
git commit -m "feat(init): _retry_loop helper com smart exit após 3 falhas"
```

---

### Task 4: Fluxo A → [1] — criar app Spotify + coletar credenciais

**Files:**
- Modify: `packages/maestra-ai/src/maestra_ai/core/init.py`
- Modify: `packages/maestra-ai/tests/unit/test_init.py`

- [ ] **Step 1: RED — teste de fluxo de coleta de creds**

```python
class TestFlowA:
    def test_collect_credentials_persist_config(self, tmp_path, monkeypatch):
        from maestra_ai.core import init, storage
        cfg_dir = tmp_path / "cfg"
        monkeypatch.setattr(storage, "config_dir", lambda: cfg_dir)

        prompts = iter([
            "s",  # Você criou o app?
            "abc123",  # client_id
            "def456",  # client_secret
            "",  # redirect_uri (Enter = default)
        ])
        monkeypatch.setattr("rich.prompt.Prompt.ask", lambda *a, **k: next(prompts))
        monkeypatch.setattr(init, "_open_url", lambda url: None)

        init._flow_A_collect_credentials()

        cfg = json.loads((cfg_dir / "config.json").read_text())
        assert cfg["client_id"] == "abc123"
        assert cfg["client_secret"] == "def456"
        assert cfg["redirect_uri"] == "https://example.com/callback"
```

- [ ] **Step 2: Verify RED**

Expected: fail.

- [ ] **Step 3: Implementar**

Em `core/init.py`:

```python
import webbrowser
from rich.prompt import Prompt


_DASHBOARD_URL = "https://developer.spotify.com/dashboard"
_DEFAULT_REDIRECT_URI = "https://example.com/callback"


def _open_url(url: str) -> bool:
    """Tenta abrir URL no navegador. Retorna True se abriu."""
    try:
        return webbrowser.open(url)
    except Exception:
        return False


def _flow_A_collect_credentials() -> None:
    """Fluxo de criação de app + coleta de creds (estado A → [1])."""
    _console.print(
        "\nPara a Maestra funcionar, você precisa criar um app no painel de "
        "desenvolvedor do Spotify. É grátis e leva ~2 minutos.\n"
    )
    opened = _open_url(_DASHBOARD_URL)
    if opened:
        _console.print(f"Acabei de abrir o painel no seu navegador.\n")
    else:
        _console.print(f"Abra no seu navegador: {_DASHBOARD_URL}\n")
    _console.print(
        "Passos no painel:\n"
        "  1) Clique em 'Create app'.\n"
        "  2) Preencha nome e descrição (qualquer coisa).\n"
        "  3) Em 'Redirect URI' cole: [bold]https://example.com/callback[/bold]\n"
        "  4) Aceite os termos e salve.\n"
        "  5) Volte aqui.\n"
    )

    while True:
        resp = Prompt.ask("Você criou o app?", choices=["s", "n"], default="s")
        if resp == "s":
            break
        _console.print("Sem pressa. Quando estiver pronto, digite 's'.")

    _console.print(
        "\nAgora vou precisar de três coisas do painel do app recém-criado.\n"
    )
    client_id = Prompt.ask("Cole o [bold]Client ID[/bold]").strip()
    client_secret = Prompt.ask(
        "Cole o [bold]Client Secret[/bold] (clique em 'View client secret')",
        password=True,
    ).strip()
    redirect_uri = Prompt.ask(
        f"Qual o [bold]Redirect URI[/bold]? [dim](Enter = {_DEFAULT_REDIRECT_URI})[/dim]",
        default=_DEFAULT_REDIRECT_URI,
    ).strip()

    if not client_id or not client_secret:
        raise ValueError("Client ID e Client Secret não podem estar vazios.")
    if not redirect_uri.startswith("https://"):
        raise ValueError("Redirect URI precisa começar com https://")

    from maestra_ai.core import auth
    auth.save_config(
        client_id=client_id,
        client_secret=client_secret,
        redirect_uri=redirect_uri,
    )
    _console.print("\n✓ Configuração salva.\n")
```

- [ ] **Step 4: Verify GREEN**

Run test. Expected: passa.

- [ ] **Step 5: Commit**

```bash
git add packages/maestra-ai/src/maestra_ai/core/init.py \
        packages/maestra-ai/tests/unit/test_init.py
git commit -m "feat(init): fluxo A — criar app + coletar credenciais"
```

---

### Task 5: Fluxo A2 → [1] — OAuth paste-back

**Files:**
- Modify: `packages/maestra-ai/src/maestra_ai/core/init.py`
- Modify: `packages/maestra-ai/tests/unit/test_init.py`

- [ ] **Step 1: RED — teste de OAuth paste-back**

```python
class TestFlowA2:
    def test_oauth_paste_back_persist_token(self, monkeypatch, capsys):
        from maestra_ai.core import init

        monkeypatch.setattr(init, "_open_url", lambda url: True)

        # Simula URL de retorno com code
        paste_url = "https://example.com/callback?code=AQD_abc123&state=xyz"
        monkeypatch.setattr("rich.prompt.Prompt.ask", lambda *a, **k: paste_url)

        saved = {}
        def fake_exchange(code):
            saved["code"] = code
            return "refresh_tok_xyz"

        monkeypatch.setattr(init, "_exchange_code_for_refresh_token", fake_exchange)
        monkeypatch.setattr(init, "_persist_refresh_token", lambda t: saved.setdefault("token", t))

        init._flow_A2_oauth_paste_back()

        assert saved["code"] == "AQD_abc123"
        assert saved["token"] == "refresh_tok_xyz"
```

- [ ] **Step 2: Verify RED.**

- [ ] **Step 3: Implementar**

Em `core/init.py`:

```python
from urllib.parse import parse_qs, urlparse


def _build_authorization_url() -> str:
    """Delega para core.auth a construção do URL de autorização."""
    from maestra_ai.core import auth
    return auth.build_authorization_url()


def _exchange_code_for_refresh_token(code: str) -> str:
    """Delega para core.auth a troca code → refresh_token."""
    from maestra_ai.core import auth
    return auth.exchange_code(code)


def _persist_refresh_token(token: str) -> None:
    from maestra_ai.core.token_store import default_token_store
    default_token_store().save(token)


def _extract_code_from_url(url: str) -> str:
    parsed = urlparse(url.strip())
    qs = parse_qs(parsed.query)
    codes = qs.get("code", [])
    if not codes:
        raise ValueError(
            "Não encontrei um parâmetro 'code' na URL. Cole a URL completa "
            "da barra de endereços do navegador, depois de clicar em 'Agree'."
        )
    return codes[0]


def _flow_A2_oauth_paste_back() -> None:
    _console.print(
        "\nAgora vou pedir autorização para acessar sua conta. Vou abrir uma "
        "página do Spotify no navegador.\n\n"
        "Você autoriza o app, o Spotify redireciona para uma URL que começa "
        "com seu Redirect URI, e você cola essa URL de volta aqui.\n"
    )
    auth_url = _build_authorization_url()
    opened = _open_url(auth_url)
    if not opened:
        _console.print(f"Abra manualmente: {auth_url}\n")

    def do_oauth() -> str:
        paste = Prompt.ask(
            "Cole a URL completa (a que começa com seu Redirect URI)"
        )
        code = _extract_code_from_url(paste)
        return _exchange_code_for_refresh_token(code)

    def classify(err: Exception) -> str:
        from spotipy.oauth2 import SpotifyOauthError  # type: ignore
        if isinstance(err, ValueError):
            return "bad_url"
        if isinstance(err, SpotifyOauthError):
            return "bad_code"
        return "unknown"

    hints = {
        "bad_url": "A URL precisa começar com seu Redirect URI e conter '?code='.",
        "bad_code": "O código expirou ou o Client ID/Secret está errado. "
                    "Gere uma nova URL de autorização no browser.",
        "unknown": "Tente novamente em alguns segundos.",
    }

    token = _retry_loop(do_oauth, classifier=classify, hints=hints)
    _persist_refresh_token(token)
    _console.print("\n✓ Autorização concluída.\n")
```

**Nota:** `auth.build_authorization_url` e `auth.exchange_code` podem não existir exatamente com esse nome. Verifique em `core/auth.py` os nomes reais; se necessário, refatore para expor esses helpers ou use os existentes (`auth.login_paste_back` pode já fazer tudo isso internamente — nesse caso, delegue diretamente). Ajuste o código conforme o real.

- [ ] **Step 4: Verify GREEN + 2 testes extras (URL sem code, code expirado)**

Adicionar:

```python
def test_paste_url_sem_code_entra_retry(monkeypatch):
    from maestra_ai.core import init
    attempts = iter([
        "https://example.com/callback?state=xyz",  # sem code
        "https://example.com/callback?code=AQD_ok",  # ok
    ])
    monkeypatch.setattr("rich.prompt.Prompt.ask", lambda *a, **k: next(attempts))
    monkeypatch.setattr(init, "_ask_retry", lambda: True)
    monkeypatch.setattr(init, "_exchange_code_for_refresh_token", lambda c: "tok")
    monkeypatch.setattr(init, "_persist_refresh_token", lambda t: None)
    monkeypatch.setattr(init, "_open_url", lambda url: True)
    init._flow_A2_oauth_paste_back()
```

- [ ] **Step 5: Commit**

```bash
git commit -am "feat(init): fluxo A2 — OAuth paste-back com extração de code + retry"
```

---

### Task 6: Fluxo B → [1] — análise de preferências

**Files:**
- Modify: `packages/maestra-ai/src/maestra_ai/core/init.py`
- Modify: `packages/maestra-ai/tests/unit/test_init.py`

- [ ] **Step 1: RED — teste end-to-end de análise (mocks pesados)**

```python
class TestFlowB:
    def test_flow_B_delega_onboard_run_e_imprime_narrativa(self, tmp_path, monkeypatch, capsys):
        from maestra_ai.core import init, onboard

        # Mock onboard.run para retornar um OnboardReport mínimo
        fake_report = {
            "playlist_id": "pl_123",
            "playlist_name": "Maestra",
            "tracks_analyzed": 150,
            "signals": {
                "top_genres": [("indie folk", 100.0), ("ambient", 80.0)],
                "dominant_decades": [("2010s", 200.0)],
                "top_artists": [("Artist A", 50.0)],
            },
            "suggestions": ["contexto A", "contexto B"],
            "rationale_path": str(tmp_path / "onboard_rationale.json"),
            "warnings": [],
        }
        monkeypatch.setattr(onboard, "run", lambda **kw: fake_report)
        monkeypatch.setattr("rich.prompt.Prompt.ask", lambda *a, **k: "Maestra")

        # Monta Spotify client fake
        class FakeSP: pass
        monkeypatch.setattr(init, "_build_spotify_client", lambda: FakeSP())

        report = init._flow_B_analysis(playlist_name_hint=None, skip_expansion=False)

        out = capsys.readouterr().out
        assert "playlist" in out.lower()
        assert "top tracks" in out.lower() or "mais ouvidas" in out.lower()
        assert "biblioteca" in out.lower() or "curtidas" in out.lower()
        assert report["action"] == "initial_analysis"
        assert report["playlist_id"] == "pl_123"
```

- [ ] **Step 2: Verify RED.**

- [ ] **Step 3: Implementar**

Em `core/init.py`:

```python
def _build_spotify_client():
    """Constrói cliente spotipy autenticado."""
    from maestra_ai.core import auth
    return auth.build_spotify_client()  # ajustar se o nome real diferir


def _flow_B_analysis(
    *,
    playlist_name_hint: str | None = None,
    skip_expansion: bool = False,
    interactive_expansion: bool = True,
) -> InitReport:
    """Executa o ciclo completo de análise (playlist + top + library + recent + expansão)."""
    from maestra_ai.core import onboard

    if playlist_name_hint is None:
        name = Prompt.ask("Nome da playlist onde as sugestões vão aparecer?",
                          default="Maestra")
    else:
        name = playlist_name_hint

    _console.print(
        f"\nVou criar uma playlist privada chamada [bold]'{name}'[/bold]. "
        "As curadorias futuras vão popular ela (~2s).\n"
    )

    sp = _build_spotify_client()

    def do_run():
        return onboard.run(
            sp=sp,
            playlist_name=name,
            dry_run=False,
            interactive=interactive_expansion and not skip_expansion,
            no_expand=skip_expansion,
        )

    def classify(err: Exception) -> str:
        from maestra_ai.core.errors import PlaylistCreateForbiddenError
        if isinstance(err, PlaylistCreateForbiddenError):
            return "403_user_management"
        return "unknown"

    hints = {
        "403_user_management": (
            "Você precisa adicionar seu email no User Management do painel "
            "Spotify. Abra o app no dashboard, vá em 'User Management' e "
            "adicione o email da sua conta."
        ),
        "unknown": "Tente novamente em alguns segundos.",
    }

    def open_user_mgmt(kind: str) -> None:
        if kind == "403_user_management":
            _open_url(_DASHBOARD_URL)

    report = _retry_loop(
        do_run,
        classifier=classify,
        hints=hints,
        on_smart_exit_link=open_user_mgmt,
    )

    _print_onboard_results(report)

    return {
        "state_before": "B",
        "action": "initial_analysis",
        "playlist_id": report.get("playlist_id"),
        "taste_profile_updated": True,
        "rationale_path": report.get("rationale_path"),
        "signals": report.get("signals"),
        "suggestions": report.get("suggestions", []),
        "warnings": report.get("warnings", []),
    }


def _print_onboard_results(report: dict) -> None:
    signals = report.get("signals", {})
    _console.print("\n[bold]Análise concluída![/bold]\n")
    if signals.get("top_genres"):
        _console.print("Gêneros dominantes: " + ", ".join(
            g for g, _ in signals["top_genres"][:5]
        ))
    if signals.get("dominant_decades"):
        _console.print("Décadas dominantes: " + ", ".join(
            d for d, _ in signals["dominant_decades"][:3]
        ))
    if report.get("suggestions"):
        _console.print("\n[bold]Sugestões de contextos:[/bold]")
        for i, s in enumerate(report["suggestions"], 1):
            _console.print(f"  {i}. {s}")
    _console.print(
        "\n[dim]Para usar:[/dim]\n"
        "  maestra context set \"<contexto>\"\n"
        "  maestra curate\n"
        "  maestra play\n"
    )
```

Os passos individuais (top tracks, biblioteca, recently, expansão) já estão internos a `onboard.run`. Se precisar narrar cada um, a alternativa é adicionar um callback `progress_hook` em `onboard.run` — mas isso é mudança maior. Para v0.8 alpha.0, `onboard.run` inteiro é uma chamada com uma mensagem antes, o que é aceitável. Deixar progress granular pra v0.8 alpha.1.

- [ ] **Step 4: Verify GREEN**

- [ ] **Step 5: Commit**

```bash
git commit -am "feat(init): fluxo B — análise de preferências delegando a onboard.run"
```

---

### Task 7: Fluxo C → [1] — atualização incremental

**Files:**
- Modify: `packages/maestra-ai/src/maestra_ai/core/init.py`
- Modify: `packages/maestra-ai/tests/unit/test_init.py`

- [ ] **Step 1: RED — sub-menu escolha [1] mood recente**

```python
class TestFlowC:
    def test_update_recent_mood(self, monkeypatch):
        from maestra_ai.core import init, onboard
        monkeypatch.setattr(onboard, "run", lambda **kw: {
            "playlist_id": "pl_x",
            "signals": {"top_genres": [("ambient", 10.0)]},
            "suggestions": [],
            "rationale_path": None,
            "warnings": [],
        })
        monkeypatch.setattr(init, "_build_spotify_client", lambda: object())

        report = init._flow_C_update(mode="recent_mood")
        assert report["action"] == "update_recent_mood"

    def test_update_full(self, monkeypatch):
        from maestra_ai.core import init, onboard
        monkeypatch.setattr(onboard, "run", lambda **kw: {
            "playlist_id": "pl_x",
            "signals": {"top_genres": [("ambient", 10.0)]},
            "suggestions": [],
            "rationale_path": None,
            "warnings": [],
        })
        monkeypatch.setattr(init, "_build_spotify_client", lambda: object())

        report = init._flow_C_update(mode="full")
        assert report["action"] == "update_full"
```

- [ ] **Step 2: Verify RED + implementar**

```python
def _flow_C_update(*, mode: Literal["recent_mood", "full"]) -> InitReport:
    from maestra_ai.core import onboard

    sp = _build_spotify_client()
    if mode == "recent_mood":
        _console.print(
            "\nAtualizando só o mood recente (últimas 4 semanas + histórico "
            "recente). Isso é rápido (~3s).\n"
        )
        report = onboard.run(
            sp=sp,
            dry_run=False,
            skip_library=True,
            skip_long_term=True,
            skip_medium_term=True,
            no_expand=True,
            skip_playlist_creation=True,
        )
        action = "update_recent_mood"
    else:
        _console.print(
            "\nRe-analisando tudo. Pode demorar ~10-20s dependendo do "
            "tamanho da sua biblioteca.\n"
        )
        report = onboard.run(
            sp=sp,
            dry_run=False,
            skip_playlist_creation=True,
            no_expand=True,
        )
        action = "update_full"

    _print_onboard_results(report)
    return {
        "state_before": "C",
        "action": action,
        "playlist_id": report.get("playlist_id"),
        "taste_profile_updated": True,
        "rationale_path": report.get("rationale_path"),
        "signals": report.get("signals"),
        "suggestions": report.get("suggestions", []),
        "warnings": report.get("warnings", []),
    }
```

**Nota sobre `onboard.run` kwargs:** `skip_library`, `skip_long_term`, `skip_medium_term`, `skip_playlist_creation` podem não existir ainda. Parte desta task é **adicionar esses parâmetros em `onboard.run`** (defaults False para manter compat) e fazer os `_fetch_*` helpers respeitarem as flags. Incluir edits em `core/onboard.py` e testes novos em `test_onboard.py`.

- [ ] **Step 3: Commit**

```bash
git commit -am "feat(init): fluxo C — update recent_mood vs full + kwargs skip_* em onboard.run"
```

---

### Task 8: Fluxos de Recomeçar (B→[2] e C→[2])

**Files:**
- Modify: `packages/maestra-ai/src/maestra_ai/core/init.py`
- Modify: `packages/maestra-ai/tests/unit/test_init.py`

- [ ] **Step 1: RED — confirma antes de apagar, apaga os artefatos certos**

```python
class TestReset:
    def test_reset_partial_apaga_token_e_config_nao_mexe_taste(self, tmp_path, monkeypatch):
        from maestra_ai.core import init, storage
        cfg_dir = tmp_path / "cfg"; cfg_dir.mkdir()
        data_dir = tmp_path / "data"; data_dir.mkdir()
        (cfg_dir / "config.json").write_text("{}")
        (data_dir / "taste_profile.json").write_text("{}")

        monkeypatch.setattr(storage, "config_dir", lambda: cfg_dir)
        monkeypatch.setattr(storage, "data_dir", lambda: data_dir)
        monkeypatch.setattr("rich.prompt.Prompt.ask", lambda *a, **k: "s")

        removed_tokens = {"n": 0}
        monkeypatch.setattr(init, "_delete_refresh_token", lambda: removed_tokens.update(n=1))

        report = init._flow_reset_partial()
        assert report["action"] == "reset_partial"
        assert not (cfg_dir / "config.json").exists()
        assert (data_dir / "taste_profile.json").exists()  # taste não tocado
        assert removed_tokens["n"] == 1

    def test_reset_full_apaga_tudo_com_confirmacao_dupla(self, tmp_path, monkeypatch):
        from maestra_ai.core import init, storage
        cfg_dir = tmp_path / "cfg"; cfg_dir.mkdir()
        data_dir = tmp_path / "data"; data_dir.mkdir()
        (cfg_dir / "config.json").write_text("{}")
        (data_dir / "taste_profile.json").write_text("{}")

        monkeypatch.setattr(storage, "config_dir", lambda: cfg_dir)
        monkeypatch.setattr(storage, "data_dir", lambda: data_dir)
        # 2 confirmações s/s
        answers = iter(["s", "s"])
        monkeypatch.setattr("rich.prompt.Prompt.ask", lambda *a, **k: next(answers))
        monkeypatch.setattr(init, "_delete_refresh_token", lambda: None)

        report = init._flow_reset_full()
        assert report["action"] == "reset_full"
        assert not (cfg_dir / "config.json").exists()
        assert not (data_dir / "taste_profile.json").exists()
```

- [ ] **Step 2: Implementar**

```python
def _delete_refresh_token() -> None:
    from maestra_ai.core.token_store import default_token_store
    try:
        default_token_store().delete()
    except Exception:
        pass


def _flow_reset_partial() -> InitReport:
    _console.print(
        "\n[yellow]Isso vai apagar a conexão com sua conta Spotify. "
        "Você vai precisar criar um app novo (ou reutilizar o atual) "
        "e autorizar de novo.[/yellow]"
    )
    if Prompt.ask("Continuar?", choices=["s", "n"], default="n") != "s":
        return _report_exit("B")

    _delete_refresh_token()
    cfg = storage.config_dir() / "config.json"
    if cfg.exists():
        cfg.unlink()
    _console.print("\n✓ Conexão apagada.\n")
    return {
        "state_before": "B", "action": "reset_partial",
        "playlist_id": None, "taste_profile_updated": False,
        "rationale_path": None, "signals": None, "suggestions": [],
        "warnings": [],
    }


def _flow_reset_full() -> InitReport:
    _console.print(
        "\n[red]ATENÇÃO: Isso vai apagar TUDO — conexão, preferências "
        "analisadas, playlist. Você vai recomeçar do zero.[/red]"
    )
    if Prompt.ask("Continuar?", choices=["s", "n"], default="n") != "s":
        return _report_exit("C")
    if Prompt.ask(
        "[red]Confirmar apagar tudo?[/red]", choices=["s", "n"], default="n",
    ) != "s":
        return _report_exit("C")

    _delete_refresh_token()
    for p in [
        storage.config_dir() / "config.json",
        storage.data_dir() / "taste_profile.json",
        storage.state_dir() / "onboard_rationale.json",
    ]:
        if p.exists():
            p.unlink()
    _console.print("\n✓ Tudo apagado. Rode `maestra init` de novo para começar.\n")
    return {
        "state_before": "C", "action": "reset_full",
        "playlist_id": None, "taste_profile_updated": False,
        "rationale_path": None, "signals": None, "suggestions": [],
        "warnings": [],
    }


def _report_exit(state: InitState) -> InitReport:
    return {
        "state_before": state, "action": "exit",
        "playlist_id": None, "taste_profile_updated": False,
        "rationale_path": None, "signals": None, "suggestions": [],
        "warnings": [],
    }
```

- [ ] **Step 3: Commit**

```bash
git commit -am "feat(init): fluxos de reset parcial (B→[2]) e total (C→[2])"
```

---

### Task 9: CLI entry + `--auto` + `--json`

**Files:**
- Create: `packages/maestra-ai/src/maestra_ai/cli/init.py`
- Modify: `packages/maestra-ai/src/maestra_ai/cli/__init__.py` (registrar subcomando)
- Modify: `packages/maestra-ai/src/maestra_ai/core/init.py` (adicionar `run_interactive`, `run_auto`)
- Modify: `packages/maestra-ai/tests/unit/test_init.py`

- [ ] **Step 1: RED — `run_interactive` disp por estado; `run_auto` cobre B e C, falha em A**

```python
class TestRunOrchestration:
    def test_run_interactive_estado_A_menu_sair(self, monkeypatch, capsys):
        from maestra_ai.core import init
        monkeypatch.setattr(init, "detect_state", lambda: "A")
        monkeypatch.setattr("rich.prompt.Prompt.ask", lambda *a, **k: "2")
        report = init.run_interactive()
        assert report["action"] == "exit"

    def test_run_auto_estado_A_falha_com_erro(self, monkeypatch):
        from maestra_ai.core import init
        from maestra_ai.core.errors import UserError
        monkeypatch.setattr(init, "detect_state", lambda: "A")
        with pytest.raises(UserError, match="requer interação"):
            init.run_auto()

    def test_run_auto_estado_B_executa_analise(self, monkeypatch):
        from maestra_ai.core import init
        monkeypatch.setattr(init, "detect_state", lambda: "B")
        monkeypatch.setattr(
            init, "_flow_B_analysis",
            lambda **kw: {"state_before": "B", "action": "initial_analysis",
                          "playlist_id": "p", "taste_profile_updated": True,
                          "rationale_path": None, "signals": None,
                          "suggestions": [], "warnings": []},
        )
        report = init.run_auto()
        assert report["action"] == "initial_analysis"

    def test_run_auto_estado_C_executa_update_full(self, monkeypatch):
        from maestra_ai.core import init
        monkeypatch.setattr(init, "detect_state", lambda: "C")
        monkeypatch.setattr(
            init, "_flow_C_update",
            lambda **kw: {"state_before": "C", "action": "update_full",
                          "playlist_id": "p", "taste_profile_updated": True,
                          "rationale_path": None, "signals": None,
                          "suggestions": [], "warnings": []},
        )
        report = init.run_auto()
        assert report["action"] == "update_full"
```

- [ ] **Step 2: Implementar orquestração em `core/init.py`**

```python
def run_interactive() -> InitReport:
    state = detect_state()
    render_menu(state)
    choice = Prompt.ask("Escolha", choices=["1", "2", "3"] if state != "A" else ["1", "2"])

    if state == "A":
        if choice == "1":
            _flow_A_collect_credentials()
            _flow_A2_oauth_paste_back()
            return _flow_B_analysis()
        return _report_exit("A")

    if state == "A2":
        if choice == "1":
            _flow_A2_oauth_paste_back()
            return _flow_B_analysis()
        if choice == "2":
            return _flow_reset_partial()
        return _report_exit("A2")

    if state == "B":
        if choice == "1":
            return _flow_B_analysis()
        if choice == "2":
            return _flow_reset_partial()
        return _report_exit("B")

    # state == "C"
    if choice == "1":
        render_update_submenu()
        sub = Prompt.ask("Escolha", choices=["1", "2", "3"])
        if sub == "1":
            return _flow_C_update(mode="recent_mood")
        if sub == "2":
            return _flow_C_update(mode="full")
        # sub == "3" → voltar; re-chama menu
        return run_interactive()
    if choice == "2":
        return _flow_reset_full()
    return _report_exit("C")


def run_auto() -> InitReport:
    state = detect_state()
    if state == "A":
        from maestra_ai.core.errors import UserError
        raise UserError(
            "Estado inicial requer interação (precisa criar app Spotify). "
            "Rode `maestra init` sem `--auto`."
        )
    if state == "A2":
        from maestra_ai.core.errors import UserError
        raise UserError(
            "OAuth paste-back requer interação. Rode `maestra init` sem `--auto`."
        )
    if state == "B":
        return _flow_B_analysis(
            playlist_name_hint="Maestra",
            skip_expansion=True,
            interactive_expansion=False,
        )
    # C
    return _flow_C_update(mode="full")
```

- [ ] **Step 3: CLI entry em `cli/init.py`**

```python
"""CLI entry para `maestra init`."""
from __future__ import annotations
import argparse
import json as _json


def register(subparsers: argparse._SubParsersAction) -> None:
    p = subparsers.add_parser(
        "init",
        help="Wizard guiado de configuração e análise (substitui onboard em v0.9).",
        description=(
            "Configura Maestra passo a passo. Detecta estado atual e continua "
            "do ponto onde você parou. Use --auto para rodar sem prompts."
        ),
    )
    p.add_argument("--auto", action="store_true",
                   help="Roda sem prompts (requer estado B ou C).")
    p.add_argument("--json", action="store_true",
                   help="Saída estruturada JSON (implica --auto).")
    p.set_defaults(func=_handle)


def _handle(args) -> int:
    from maestra_ai.core import init as init_mod

    auto = args.auto or args.json
    try:
        if auto:
            report = init_mod.run_auto()
        else:
            report = init_mod.run_interactive()
    except init_mod.UserAbort as e:
        if args.json:
            print(_json.dumps({"error": str(e)}))
        return 1

    if args.json:
        # Converte InitReport para JSON
        print(_json.dumps(dict(report), ensure_ascii=False, default=str))
    return 0
```

- [ ] **Step 4: Registrar no parser principal**

Em `packages/maestra-ai/src/maestra_ai/cli/__init__.py`, adicionar:

```python
from maestra_ai.cli import init as init_cli
# ...
init_cli.register(subparsers)
```

Siga o padrão dos outros subcomandos no arquivo.

- [ ] **Step 5: Verify GREEN**

```bash
uv run pytest packages/maestra-ai/tests/unit/test_init.py -v
uv run maestra init --help  # smoke: help aparece
```

- [ ] **Step 6: Commit**

```bash
git commit -am "feat(init): orquestração run_interactive/run_auto + CLI entry + --json"
```

---

### Task 10: Deprecação de `onboard` + help topics

**Files:**
- Modify: `packages/maestra-ai/src/maestra_ai/cli/onboard.py`
- Create: `packages/maestra-ai/src/maestra_ai/docs/topics/init.md`
- Modify: `packages/maestra-ai/src/maestra_ai/docs/topics/onboarding.md`
- Modify: `packages/maestra-ai/tests/unit/test_onboard_cli.py` (se não existir, criar)

- [ ] **Step 1: RED — teste que rodar `maestra onboard` imprime warning**

```python
def test_onboard_deprecation_warning_stderr(capsys, monkeypatch):
    from maestra_ai.cli import onboard as onboard_cli
    import argparse

    # Stub o handler real para não executar de verdade
    monkeypatch.setattr(onboard_cli, "_run_onboard_real", lambda args: 0)

    args = argparse.Namespace(
        playlist_name="Maestra", playlist_id=None,
        seed_playlist=30, dry_run=True, yes=True,
        non_interactive=True, total_cap=5000, no_expand=True,
        expand_playlists=None, json=False,
    )
    rc = onboard_cli.cmd_onboard(args)
    err = capsys.readouterr().err
    assert "depreciado" in err.lower() or "deprecated" in err.lower()
    assert "maestra init" in err
    assert rc == 0
```

- [ ] **Step 2: Implementar warning**

Em `packages/maestra-ai/src/maestra_ai/cli/onboard.py`, no início do handler principal:

```python
from rich.console import Console
_err = Console(stderr=True)


def cmd_onboard(args):
    _err.print(
        "[yellow]⚠ `maestra onboard` está depreciado e será removido em v0.9. "
        "Use `maestra init` (interativo) ou `maestra init --auto` (scripted).[/yellow]"
    )
    return _run_onboard_real(args)


def _run_onboard_real(args):
    # ... lógica atual ...
```

Refatore o código atual para mover o corpo de `cmd_onboard` (antes da mudança) para `_run_onboard_real`. Isso facilita testes.

- [ ] **Step 3: Help topic `init.md`**

Em `packages/maestra-ai/src/maestra_ai/docs/topics/init.md`:

```markdown
# Init

Wizard guiado de configuração e análise da Maestra. Substitui o fluxo antigo
`auth setup` + `auth login` + `onboard` por um único comando.

## Modos

- `maestra init` — interativo. Detecta estado (zero, conectado, tudo pronto)
  e oferece menu contextual.
- `maestra init --auto` — sem prompts. Requer estado B ou C (conexão já feita).
- `maestra init --json` — saída JSON estruturada (implica `--auto`).

## Estados

| Estado | Significa | Ação padrão |
|---|---|---|
| A | Nada configurado | Criar app + autorizar + analisar |
| A2 | App criada, sem autorização | Autorizar + analisar |
| B | Conectado, sem análise | Analisar preferências |
| C | Tudo pronto | Atualizar preferências |

## Primeira vez (estado A)

1. Crie um app no dashboard Spotify em https://developer.spotify.com/dashboard
   (o wizard abre pra você).
2. Em "Redirect URI", cole: `https://example.com/callback` (TLD reservado,
   não precisa hospedar nada).
3. Cole Client ID e Client Secret no wizard.
4. Autorize o app no navegador; cole a URL de retorno no wizard.
5. Escolha o nome da playlist onde as sugestões vão aparecer (default: Maestra).
6. Aguarde a análise (~10-30s dependendo da biblioteca).

## Troubleshooting

### 403 ao criar playlist

Apps em Development Mode exigem que seu email esteja em "User Management"
no painel. O wizard detecta esse erro após 3 tentativas e oferece abrir o
painel pra você.

### URL paste-back rejeitada

A URL precisa começar com seu Redirect URI (`https://example.com/callback`)
e conter `?code=...`. Cole a URL completa da barra de endereços DEPOIS de
autorizar o app.

### `--auto` falha em estado A

Estado A requer interação pra criar app Spotify. Rode `maestra init` sem
`--auto` na primeira vez.
```

- [ ] **Step 4: Banner em `onboarding.md`**

No topo do arquivo existente `docs/topics/onboarding.md`, adicionar:

```markdown
> ⚠️ **Depreciado.** `maestra onboard` será removido em v0.9. Use
> `maestra init` (ou `maestra init --auto` em scripts).
> Veja `maestra help init`.

```

(Manter o resto do conteúdo por ora — quem depende do help antigo ainda encontra.)

- [ ] **Step 5: Verify + commit**

```bash
uv run pytest packages/maestra-ai/tests/unit/test_onboard_cli.py -v
git commit -am "feat(init): deprecação de onboard + help topics (init.md + banner em onboarding.md)"
```

---

### Task 11: Integration tests

**Files:**
- Create: `packages/maestra-ai/tests/integration/test_init_flow.py`

- [ ] **Step 1: Fluxo A → B → C end-to-end via subprocess mockando Spotify**

```python
"""Integration tests for maestra init full flow."""
from __future__ import annotations
import json
import subprocess
import pytest
from pathlib import Path


@pytest.mark.integration
def test_init_auto_estado_B_produz_taste_profile(tmp_path, monkeypatch):
    """Com creds + token, init --auto --json gera taste_profile."""
    # Preparar ambiente
    env = {
        "MAESTRA_CONFIG_DIR": str(tmp_path / "cfg"),
        "MAESTRA_DATA_DIR": str(tmp_path / "data"),
        "MAESTRA_STATE_DIR": str(tmp_path / "state"),
        "PATH": "/usr/bin:/bin",
    }
    (tmp_path / "cfg").mkdir(parents=True)
    (tmp_path / "cfg" / "config.json").write_text(json.dumps({
        "client_id": "test_id", "client_secret": "test_secret",
        "redirect_uri": "https://example.com/callback",
    }))

    # Mock keyring com token "fake_tok" e Spotify responder via VCR ou fixture
    # (deixar como skeleton; implementação detalhada depende de fixtures atuais)
    pytest.skip("Integration stub — requer fixtures VCR para Spotify API")
```

A integration test full requer infraestrutura de fixtures de rede que pode não existir. **Deixar como skeleton + skip** por ora; o subagent pode expandir quando houver fixtures. O essencial de cobertura está nos unit tests.

- [ ] **Step 2: Teste real de CLI smoke**

```python
def test_cli_init_help_nao_quebra():
    """`maestra init --help` imprime ajuda e sai com 0."""
    result = subprocess.run(
        ["uv", "run", "maestra", "init", "--help"],
        capture_output=True, text=True, cwd=str(Path(__file__).parents[3]),
        timeout=30,
    )
    assert result.returncode == 0
    assert "wizard" in result.stdout.lower() or "init" in result.stdout.lower()
```

- [ ] **Step 3: Commit**

```bash
git commit -am "test(init): integration tests skeleton + smoke test CLI"
```

---

### Task 12: Bump v0.8.0-alpha.0 + CHANGELOG + tag

**Files:**
- Modify: `packages/maestra-ai/pyproject.toml`
- Modify: `packages/maestra-mcp/pyproject.toml`
- Modify: `CHANGELOG.md`

- [ ] **Step 1: Bump versions**

`packages/maestra-ai/pyproject.toml`: `version = "0.8.0a0"` (formato PEP 440).
`packages/maestra-mcp/pyproject.toml`: `version = "0.8.0a0"`.

- [ ] **Step 2: CHANGELOG**

Adicionar topo de `CHANGELOG.md`:

```markdown
## [0.8.0-alpha.0] — 2026-04-20

### Adicionado
- `maestra init` — wizard guiado de configuração unificado. Detecta estado
  (A / A2 / B / C) e apresenta menu contextual. Linguagem sem jargão,
  tom caloroso, retry loop com smart exit após 3 falhas.
- Flags `--auto` (script mode) e `--json` (para agentes LLM).
- Help topic `maestra help init`.
- Módulo `core/init.py` com state machine linear + `core/init_types.py` com
  `InitState` + `InitReport`.

### Deprecado
- `maestra onboard` — warning ao rodar. Será removido em v0.9. Migração:
  `maestra init` (interativo) ou `maestra init --auto` (scripted).

### Contratos preservados
- `taste_profile.json`, `onboard_rationale.json` inalterados.
- Tool MCP `onboard_rationale` continua funcionando (lê artefato gerado por
  `init` do mesmo jeito que antes por `onboard`).
```

- [ ] **Step 3: Verify final**

```bash
uv run pytest -q
uv run ruff check packages/
uv run maestra init --help  # smoke
```

Expected: ≥580 passed (564 + ~20 novos de test_init), ruff limpo.

- [ ] **Step 4: Commit + tag**

```bash
git add -A
git commit -m "chore: bump v0.8.0-alpha.0 — maestra init (front door unificado)"
git tag v0.8.0-alpha.0
```

---

## Self-review

**Spec coverage:**
- Detecção de estado (4 estados): T1 ✓
- Menu rendering: T2 ✓
- Retry loop + smart exit: T3 ✓
- Fluxo A: T4 ✓
- Fluxo A2: T5 ✓
- Fluxo B: T6 ✓
- Fluxo C + sub-menu: T7 ✓
- Reset (B→[2], C→[2]): T8 ✓
- CLI + `--auto` + `--json`: T9 ✓
- Deprecação + help topics: T10 ✓
- Integration tests (skeleton): T11 ✓
- Bump + tag: T12 ✓

**Placeholder scan:** sem TBD. Uma nota explícita em T11 (integration test full fica skeleton + skip, unit tests cobrem o essencial) — é decisão, não placeholder.

**Type consistency:**
- `InitState = Literal["A", "A2", "B", "C"]` — usado em T1, T2, T9.
- `InitReport` — keys consistentes em T6, T7, T8, T9.
- `_flow_A_collect_credentials`, `_flow_A2_oauth_paste_back`, `_flow_B_analysis`, `_flow_C_update(mode=...)`, `_flow_reset_partial`, `_flow_reset_full` — nomes alinhados entre definição (T4-T8) e chamada (T9).

**Ambiguidades potenciais:**

- T6 nota que narração granular por etapa fica pra alpha.1. Aceitável.
- T7 menciona que `skip_*` kwargs em `onboard.run` podem precisar ser adicionados. Essa é uma mudança adjacente ao escopo — incluída explicitamente na task.
- T11 skip de integration full — justificado por fixtures ausentes.

**Notas de execução:**

- T4, T5, T6 dependem de funções reais em `core/auth.py` (`save_config`, `build_authorization_url`, `exchange_code`, `build_spotify_client`). O subagent dessas tasks deve inspecionar `core/auth.py` primeiro e adaptar os nomes; se algum helper não existir, criar como parte da task.
- T10 assume que `cli/onboard.py` tem `cmd_onboard` como handler principal. Se a função tem outro nome (ex: `_handle`), ajustar.
- Todas as tasks usam pytest-asyncio modo auto (configurado no root). Testes async não precisam de `@pytest.mark.asyncio`.

**Tamanho total:** ~12 tasks, ~25-30 commits esperados. Release tamanho médio (similar a v0.7.0). Subagents de implementação são os mais adequados (fresh context por task).
